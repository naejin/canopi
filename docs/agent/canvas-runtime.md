# Canvas Runtime

Use this guide when changing canvas state, scene runtime, renderer behavior, hit testing, selection, presentation, history, Target projection, or Canvas2D tab renderers.

## Public Seams

- App code must not reach into renderer implementations or runtime internals.
- The app-facing canvas boundary is split into `CanvasCommandSurface`, `CanvasQuerySurface`, and `CanvasDocumentSurface`.
- `CanvasCommandSurface` is role-shaped: `tools`, `viewport`, `history`, `sceneEdits`, `chrome`, `layers`, and `plantPresentation`. New focused callers should consume the narrow role surface exported by `canvas/session.ts` instead of the full command bundle.
- The App Command Graph may consume the full `CanvasCommandSurface` because it is the shared command-ID orchestration layer. Do not use that as precedent for ordinary UI components or leaf controllers.
- Toolbars, shortcuts, menus, and plant color actions consume command surfaces through the narrowest role that covers their writes.
- `app/canvas-commands/` owns the platform-neutral Canvas toolbar command catalog and caller-shaped projection: the exact 2/6/2 tool groups, tool/history/settings command identity, resolved labels and descriptions, shortcuts, active/disabled/pressed policy, and typed intents. Desktop adapts that catalog into the App Command Graph; Web Edition builds the same projection from browser-safe runtime facts. Keep Tauri, Web modules, platform entries, IPC, storage, and shell controllers outside this module; icons and Plant Color/Plant Symbol popovers remain presentational toolbar concerns.
- A disabled projected command must not dispatch. Edition adapters must also re-read the live Canvas command surface when an admitted action runs so a retained DOM handler cannot target a detached runtime; do not close over a rendered command surface for later mutation.
- Document session orchestration, save/load, chrome, resize, and teardown consume document surfaces.
- Sibling read-only surfaces consume query surfaces for scene snapshots, the viewport snapshot signal, selection reads, placed plants, localized names, and presentation context.
- `CanvasQuerySurface.getDesignObjectSelection()` is the app-facing read model for editable top-level Design Object selection, selected locked Design Objects, blocked selection reasons, and combined visual bounds. New selection actions, transform overlays, lock cues, and toolbar placement should consume this query instead of duplicating plant Visual Footprint, annotation bounds, Zone bounds, Layer, Object Group, or Design Object Lock rules.
- Publish live canvas sessions as explicit `CanvasRuntimeSurfaces` bundles. Do not publish a raw `SceneCanvasRuntime`, and do not cast a role-specific fake into the session signal; tests should use `createTestCanvasRuntimeSurfaces()` with the role surface they need.
- `createCanvasRuntimeSurfaces()` must compose `runtime.commandSurface`, `runtime.querySurface`, and `runtime.documentSurface` directly. Do not reintroduce fallback casts that treat `SceneCanvasRuntime` itself as any role surface.
- Scene and localized Plant-name freshness for app callers is exposed through `CanvasQuerySurface.revision`. Do not recreate canvas runtime mirror revision signals; app modules subscribe through the query surface instead.
- `CameraController.snapshot` is the sole transient viewport authority. It atomically publishes one immutable viewport, screen size, reference scale, and monotonically increasing revision; no-op camera mutations keep the same snapshot and revision. `CanvasQuerySurface.viewport` forwards that exact signal for app callers. Do not mirror viewport facts or revisions into Scene Session state, global signals, or query aliases.
- `canvas/runtime/runtime.ts` owns the app-facing Canvas Runtime Surface interfaces. `canvas/runtime/surfaces.ts` only composes the live runtime surfaces; command, query, and document surface behavior belongs behind the role modules in `canvas/runtime/*-surface.ts`.
- `SceneCanvasRuntime` may still wire the concrete scene runtime during migration, but new surface behavior should be added to the relevant role module before adding more broad runtime pass-through code.
- `canvas/runtime/scene-runtime/construction.ts` owns concrete runtime construction: SceneStore, CameraController, SceneHistory, the Settled Scene Authority, RendererHost, presentation, document bridge, mutation controller, and command/query/document role surface assembly. It returns only a `SceneStateReader`, a session-only writer, and edit/admission roles to the `SceneCanvasRuntime` shell; never leak the concrete Store or the full multi-role authority through the construction result. Keep new construction and disposal wiring behind that module instead of expanding `SceneCanvasRuntime`.
- `canvas/runtime/document-surface.ts` owns document lifecycle surface behavior for load, replace, exact persistence capture, viewport initialization, chrome attachment, resize, and teardown. Its document authority state is `absent`, `settling`, or `loaded`: once hydration begins, `hasLoadedDocument()` remains true even after a late failure, while persistence capture reports typed `document-settlement` busy until the retained hydration succeeds. A coordinator-issued `CanvasDocumentReplacementNotAdmittedError` is the sole exception: hydration never began, so the surface restores its prior document state and the old Scene remains captureable. This prevents store-only fallback from hiding an already accepted Scene. `SceneCanvasRuntime` compatibility methods delegate to that role.
- `canvas/runtime/lifecycle-owner.ts` owns the single native-or-Web runtime lifetime lease. Component cleanup requests release through that owner; if loss-prevention handoff fails, the owner retains the release callback and retries it before another runtime may mount. After handoff succeeds, component cleanup must exhaust every remaining independent cleanup and release this lease even when a non-authoritative cleanup reports an error.
- `canvas/runtime/query-surface.ts` owns read-only surface behavior for scene snapshots, viewport facts, revisions, selection, placed plants, localized names, and plant presentation context. `SceneCanvasRuntime` compatibility methods delegate to that role.
- `canvas/runtime/command-surface.ts` owns command surface behavior for tools, viewport changes, history, scene edits, chrome toggles, layer edits, and plant presentation writes. `SceneCanvasRuntime` compatibility methods delegate to that role.
- `SceneCanvasRuntime` is the compatibility shell for initialization, interaction attachment, and private callbacks used by construction; it should not regain role-surface dependency assembly or low-level constructor wiring.
- `app/canvas-runtime/host.ts` owns desktop live runtime creation and publication of `CanvasRuntimeSurfaces` into the Design Session.
- `web/browser-canvas-runtime.ts` owns Web Edition runtime creation. It must stay browser-safe: no Tauri IPC, no desktop document lifecycle, no command graph, and no native Saved Object Stamp import/export path.
- `web/WebCanvasWorkspace.tsx` must not import desktop canvas chrome such as `LayerPanel`; those surfaces transitively pull command graph, settings persistence, location index, and Tauri IPC paths into the browser bundle. Add browser-specific replacements under `desktop/web/src/web/` when those affordances return.
- `canvas/runtime/app-adapter.ts` owns the runtime-facing `CanvasRuntimeAppAdapter` contract and detached fallback behavior.
- `app/canvas-runtime/app-adapter.ts` owns production wiring from the adapter contract to app-owned document/session/settings modules.
- `canvas/runtime/presentation-data.ts` owns the runtime-facing Species presentation interfaces. Production presentation-data sources should enter construction through `CanvasRuntimeAppAdapter.presentationData` or explicit test overrides. Desktop app adapters provide IPC-backed `CanvasPlantLabelResolver` and `CanvasSpeciesCache`; web hosts use detached/browser adapters until the DuckDB-WASM catalog supplies real data.
- Production files under `canvas/runtime/` must not import `app/**`; add behavior through `CanvasRuntimeSurfaces`, `CanvasRuntimeAppAdapter`, or a narrower runtime-owned interface instead.

## Scene Ownership

- `SceneStore` owns canvas scene state: plants, zones, annotations, groups, Design Object locks, layers, plant species colors, plant species symbols, selection, hover, and plant presentation state. Transient viewport state belongs only to `CameraController`.
- Scene Design Object identity is always the immutable typed pair `{ kind, id }`; the neutral definitions and identity helpers live in `canvas/runtime/scene/design-object-targets.ts`, not in a kind-specific grouping or interaction module. Plant IDs, Zone names, Annotation IDs, Measurement Guide IDs, and Object Group IDs are not globally unique across kinds. Scene Session stores `selectedTargets` and `hoveredTarget`; commands, history patches, queries, interaction hit results, locks, and renderer presentation must preserve the pair and must never infer kind by searching a raw ID in a preferred order.
- `CanvasQuerySurface.getSelection()` returns an owned typed-target snapshot. The app-level `selectedObjectIds` signal is intentionally a lossy UI notification mirror: it projects only raw IDs for legacy reactive chrome and may collapse cross-kind collisions, but it must still publish when typed membership changes without changing that raw projection. Never read it as selection authority or use it to derive mutation targets, selection counts, hover, locks, history, or renderer state.
- Store mutators expose temporary mutable drafts only for the duration of their callback. `SceneStore` must clone the completed persisted or Session draft before installing it so a retained draft, nested entity, or collection cannot mutate authoritative state after admission closes.
- `SceneStore` is also the live-preview read model during a direct-manipulation gesture. `SceneRuntimeEditCoordinator` in `scene-runtime/transactions.ts` is the Settled Scene Authority: it exclusively admits persisted Scene and selection commands, records Scene history, replays undo/redo, applies presentation backfills, and hydrates documents.
- Consumers receive narrow authority roles for Scene Edit transactions, settled-command admission, settled observation, history, presentation maintenance, document hydration, or persistence capture. Read-side tools receive `SceneStateReader` or `SettledSceneReader`, never the concrete `SceneStore`. `SettledSceneReader.readWhenSettled()` only observes admission and returns its busy value; it must not flush maintenance or recover retained commands. Query surfaces and computed command availability such as undo/redo and paste must use that observational role, not `SceneCommandAdmission`. Keep the concrete authority and Store confined to runtime construction/authority ownership; do not pass `SceneCommandAdmission`, `SceneHistory`, or raw persisted Scene writers to query modules.
- Exactly one authority operation may be open or finalizing: a Scene Edit, one exact undo/redo replay, document hydration, or reserved document replacement. Immediate edits return busy without invoking their callback, a second long-lived begin raises `SceneEditBusyError`, history availability is false while busy, and whole commands must cross `runWhenSettled()` before reading live preview state or changing clipboard/selection state. Retained settlement recovery is a separate quarantined admission step: it must report the current command busy/false and must never use a broad history type as identity or success for a later command. Command callers may request pending immediate settlement retry; read-only/computed callers must not. Do not expose or perform a separate public active-edit check followed by a raw Scene write.
- Abort restores only persisted Scene state and transaction-owned selection. Camera viewport, hover, app-owned active Layer settings, and other transient presentation state may continue changing during a live preview and must not be rewound by abort.
- A failed abort remains admitted and retryable. A commit failure before history accepts the transaction may abort to its before-snapshot; once history accepts it, later projection, revision, and render publication retries must finish that committed outcome without recording twice or permitting the opposite rollback. Immediate commands retain an authority-owned retry handle so a failure cannot strand an inaccessible transaction.
- Tool and command cleanup that is valid only after a changed commit must use the transaction's `onCommitted` continuation. The authority runs that continuation after every other commit publication phase and before releasing ownership, and retries it with the retained transaction after failure. Keep continuations idempotent because an exception can occur after a continuation has made a partial external change. After a successful continuation, the authority performs a separate retryable deferred-backfill drain before release; a backfill enqueued by the continuation must not remain hidden in the queue or cause the continuation to run twice when publication retries.
- Undo/redo cursor movement, Scene patch replay, projection synchronization, revision publication, and invalidation form one staged authority settlement. A failure keeps the same history entry admitted; retry must not replay a different entry or expose partially restored state to another command.
- UI mirrors such as selection, Layer settings, guides, and Plant species colors synchronize as an idempotent authority settlement phase after commit or restored abort. Do not publish those mirrors from inside a speculative transaction callback or from a command after `run()` returns.
- Presentation metadata backfills use opaque authority tickets containing document generation, authoritative content revision, and per-Plant Canonical Name/presentation lineage. Defer current tickets while authority or backfill publication is busy, apply them after abort/no-op settlement, and drop them after changed commit, undo/redo, or document hydration makes them stale. Revision/invalidation publication is part of the reactive authority lifecycle: its callbacks must not re-enter a Scene command, and settled readers must be republished when maintenance finishes. Commit continuations and replacement finalizers run while authority remains admitted, so drain again after those callbacks before release. Render-time asynchronous presentation refresh prepares cache results without publishing query revisions or Scene backfills; `SceneRuntimeRenderScheduler` checks its container/epoch after preparation, then publishes the prepared render synchronously, and checks again before rendering. Plant-name cache preparation uses a monotonic prepared/published revision, so a newer cached refresh can publish an unpublished change when the refresh that populated labels becomes stale. Publication reports whether it advanced that revision; command owners must include that result in return and scene-invalidation semantics even when their cache result otherwise says unchanged. If a later Species-cache stage fails, retain the prepared label progress: an active command owner publishes and invalidates it before surfacing the failure, while render preparation skips unavailable backfills and renders the label-enriched snapshot without creating a fire-and-forget rejection. A stale or destroyed owner must publish nothing. Disposal invalidates the render epoch and marks the runtime inactive before other cleanup; render and command refreshes resolving after teardown may populate caches but must not mutate Scene state or publish query revision/invalidation. Backfill maintenance does not advance authoritative content revision, so sibling tickets from the same content may enrich disjoint Plants; the first sibling accepted for one Plant identity makes overlapping siblings stale, while preview and committed Canonical identities keep separate reservations until settlement chooses one. Retain accepted presentation metadata across history removal/restoration of the Plant. Validate both Plant identity and Canonical Name before applying an asynchronous backfill.
- Document replacement reserves a successor authority operation before canceling Scene Interaction or clearing fallible Target presentation. A live gesture's release hands authority directly to that successor without publishing an open slot; a cleanup failure before hydration removes the reservation, emits `CanvasDocumentReplacementNotAdmittedError`, and leaves the old Scene authoritative. Design Session must abandon that token and pending finalizer, while ordinary failures after `beginHydration()` retain them. Retained immediate/history recovery quarantines the replacement attempt, which must be retried. Once the Scene swap succeeds, invalidate old tickets immediately and keep replacement admitted through history clearing, projection, revision, render publication, and the caller's replacement finalizer. Replacement hydration alone owns the history clear and releases only after that idempotent finalizer; callers must not clear history or reset cross-authority baselines after release. The document surface requires an opaque logical replacement token. Design Session retains it across retries of one normalized input identity, including kind, path, and name; a retry resumes the original logical finalizer and never registers the retry call's fresh closure. That finalizer resets baselines and replaces Design identity as one batched stage, recording completion before reactive publication so a post-apply exception cannot make retry erase a later non-canvas edit. Equivalent file content carrying another token is a competing replacement: its triggering call may settle the retained operation, but is quarantined and cannot claim that operation's receipt. The document surface receipt reports whether the current caller's finalizer ran, allowing Design Session to acknowledge an already-finalized retained replacement without repeating state replacement or dirty-baseline reset. The authority owns an immutable snapshot of the accepted input and passes a fresh copy to each fallible projection attempt, so a retry remains stable when normalization produced a new object identity or caller/projection code later mutates an object it received.
- `CanvasDocumentSurface.captureForPersistence()` is the only public Scene save seam. It returns defensively owned composed content, exact-current observation, and an opaque, idempotent `acknowledgeSaved()` closure. Exact-current identity covers history/document/runtime identity plus the serialized presentation projection; keep those private and do not reintroduce separate public serialize and mark-saved calls.
- `CanvasAuthorityBusyError` is the app-facing contract for a temporarily unavailable exact Canvas capture. Design Session persistence may coordinate a deferred guard through that contract, but must not import concrete `SceneEditBusyError` or other Scene authority implementation types.
- Persistence capture during an open Scene Edit serializes the committed before-state while the live preview remains visible. Once history accepts a commit, reentrant capture serializes the committed after-state even if later publication phases are retrying. A retained undo/redo replay, hydration/replacement, or active presentation publication rejects capture as busy because Scene content and checkpoint identity may be between settlement phases.
- Acknowledgement revalidates document generation and runtime lifetime both before and after clean-state publication, because that callback may synchronously replace or dispose the runtime. Runtime disposal permanently closes persistence authority: existing closures become stale and a retained destroyed document surface cannot issue a fresh capture.
- Presentation backfills are non-dirty semantic enrichment. They may change serialized presentation metadata without creating a new history state identity, so they advance exact persistence-projection identity without dirtying history. Acknowledgement and undo cleanliness follow admitted semantic edits, not byte equality of derived cache data.
- Cross-authority workflows that write Design state from Scene data must consume a settled query such as `getSettledPlacedPlants()` and skip work while it returns `null`. Read-only visual projections may continue to use live-preview queries.
- Non-canvas document sections are not owned by `SceneStore`: consortiums, timeline, budget, `budget_currency`, location, description, and document extra.
- Historical wire optionality stops at Scene hydration. `ScenePersistedState` and renderer snapshots are total internal contracts, so runtime consumers must not repeat missing-array fallbacks such as `?? []`; tests should construct total snapshots through shared builders.
- The active Layer setting is app-owned Canvas Settings state, not Scene Session state. Layer visibility, opacity, and locks remain persisted Scene edits.
- Commands, tools, save/load, and document replacement mutate scene state, not renderer objects.
- Canvas-owned document fields serialize from the live scene, not stale document input copies.
- Full Design file composition is app-owned and crosses `CanvasRuntimeAppAdapter.document`. Runtime core serializes canvas-owned state and must not import `app/contracts/document`.
- Plant presentation state lives in `SceneStore.session`, not standalone canvas signals.
- Scene Layer visibility, opacity, and locks are scene edits. UI controllers must call the canvas command surface instead of writing `layerVisibility`, `layerOpacity`, or `layerLockState` directly.
- Canvas Layer Presentation (`app/canvas-layer-presentation/presentation.ts`) owns the visible Layer catalog and authority-correct row commands for scene Layers, basemap, contours, and hillshading. UI callers should consume that seam instead of hard-coding `base`, `contours`, or `hillshading` routing.
- Layer lock controls belong on scene Layer rows in the Layer Panel and must write through Canvas Layer Presentation. Layer-locked objects can show locked-Layer hover cues, but must not show object-level unlock affordances.
- Design Object lock state lives on persisted scene entities in `SceneStore`. Production code must read and mutate object locks through scene queries/edits, never through standalone mirror signals.
- Object Groups are flat and may span the Plants, Zones, and Annotations Layers. Grouping a selected Object Group with other editable Design Objects should merge the group's members into one flat Object Group, not create a nested group. When exactly one Object Group participates, preserve that group's identity, name, and selection. When multiple Object Groups participate, the topmost selected Object Group in scene order survives with its identity, name, and direct lock state; merge into that surviving Object Group and remove the other selected group records.
- Grouping should be idempotent over selection edge cases: expand selected Object Groups into concrete members, add selected concrete Design Objects, deduplicate by typed identity, and proceed only when at least two concrete members remain.
- Object Group Layer behavior and geometry are derived from member Design Objects. Object Groups should persist identity, optional name, lock state, and typed concrete member identity only; they should not persist standalone Layer, position, or rotation fields. Runtime code must not use standalone Object Group Layer or transform fields as authority for visibility, locking, hit testing, selection, mutation eligibility, bounds, pivots, or placement.
- Object Group membership must be typed so Placed Plant IDs, Zone names, and Annotation IDs cannot collide. Persist members as concrete Design Object references such as Plant, Zone, and Annotation; do not persist Object Group members inside Object Groups. Grouping existing groups should expand them into concrete members.
- Migrating legacy Object Groups with raw `member_ids` must resolve a raw ID only when it matches exactly one concrete Design Object. Drop ambiguous or missing migrated members, report the migration issue through the document-load problem surface, and drop any migrated Object Group that has fewer than two resolved members.
- Cross-Layer Object Groups are editable only when all existing member Design Objects are on visible, unlocked Layers. If any member Layer is hidden, the group should not be selected as a group from canvas hit testing; visible members of that group remain structurally blocked from individual selection so users cannot partially edit a group with hidden members. If any member Layer is locked, mutation commands for the group must be unavailable and the group should be treated as a structural blocker rather than an object-level Unlock target.
- Ungrouping an Object Group should select the resolved concrete former members that are visible and editable after the group record is removed. Missing stale members are ignored.
- A group is effectively locked for mutating canvas interactions when the group itself is locked, any existing member Design Object is locked, or any existing member Design Object is on a locked Layer. Missing member IDs do not lock a group.
- Directly locked Design Objects stay hover-discoverable and can become selected locked targets so the Selection Action Toolbar can offer Unlock. They must not become editable targets until unlocked; other mutation buttons should be visible but disabled in the locked-selection toolbar state. Locked-Layer objects, hidden objects, grouped members, missing targets, and Object Groups blocked only by locked members remain structural blockers rather than selected locked targets. Do not reintroduce implicit unlock-all behavior through `unlockSelected()` or shortcuts.
- Movement, color, and other mutating commands must derive write targets from the typed editable-selection model or explicit Layer/lock filters. Broad Species scopes may include blocked targets for read-only affordances and must not be used directly for Scene mutations. Lock reads and writes accept typed targets; do not add raw-ID lock helpers.
- Guide creation is a scene edit owned by the runtime. The `guides` signal is a chrome projection, not document authority.
- Runtime chrome rendering and interaction snapping read guides from `SceneStore.persisted.guides`; do not read the `guides` signal in production runtime behavior.
- Canvas signals such as selected object IDs, layer state, and guides are UI mirrors, not runtime authority.
- Canvas Query Surface revision values are the app-facing freshness seam for scene entities and localized Plant names. Viewport callers subscribe to the atomic `CanvasQuerySurface.viewport` snapshot instead; do not recreate `runtime-mirror-state.ts`, viewport mirrors, or mirror revision signals.
- Canvas Map Surface reads saved Location and `north_bearing_deg` through the Location Workbench presentation seam, not `scene-metadata-state` or direct document-session imports.
- `canvas/runtime/scene-physical-extent.ts` owns the pure radial physical extent of the Scene from design origin `(0, 0)`. It includes Placed Plant centers, effective Zone geometry, Annotation anchors, and both Measurement Guide endpoints; it deliberately excludes screen-readable Plant and Annotation Visual Footprints. It delegates every Zone shape through `zone-geometry.ts#getZoneRadialExtentMeters`; keep line, polygon, rotated rectangle, and rotated ellipse dispatch behind that one interface, including the true Elliptical Zone radial extremum rather than sampled perimeter points or an axis-aligned bounds corner.
- Basemap, contour, and hillshade display settings remain app settings projections; do not route those map-surface settings through Scene Edit unless the document schema intentionally makes them canvas-owned.
- Shared runtime settings reads, writes, and subscriptions for locale, theme invalidation, grid/rulers/snap, Plant Spacing interval, and layer projections cross `CanvasRuntimeAppAdapter.settings`. Runtime core must not import `app/settings/*` or `app/canvas-settings/*` for those concerns.
- No circular imports between scene store/runtime and document store. Pass document readers or values through seams when needed.

## History And Dirty State

- `SceneHistory` dirty state uses unique state identities. A checkpoint names the exact current identity: acknowledging it can leave later commands dirty, undo to it becomes clean, and a divergent branch at the same stack depth remains dirty.
- `SceneHistory` is a private authority collaborator. Command callers consume only `SceneHistoryCommands`; persistence consumes an opaque closure issued by `ScenePersistenceAuthority`. Neither surface may execute raw history operations or obtain a history runtime that can write `SceneStore`.
- `SceneHistory` reports canvas clean-state through an injected clean-state callback from the `CanvasRuntimeAppAdapter`. Production wiring lives in `app/canvas-runtime/app-adapter.ts`; runtime core must not import `app/document-session/store`.
- Clearing history advances its checkpoint generation and makes older checkpoints stale. Runtime disposal separately invalidates captures so acknowledgements cannot publish into a later runtime lifetime.
- Any history truncation or branch behavior must preserve unique saved-state identity in all write paths; never derive cleanliness from past-stack length. If the saved identity remains reachable at the truncation boundary, undoing to it must still become clean.
- Selection, hover, presentation-only target highlights, labels, and viewport-only work must not create document history entries unless explicitly designed.
- Auto-fit on attached document open is expected; attached Design Session transitions call `zoomToFit()` after hydration. Detached transitions must not call canvas-only document surface methods.

## Rendering Ownership

- `RendererHost` owns backend lifecycle, capability probing, fallback, and pending backend initialization cancellation. It publishes the shared initialization Promise before invoking backend code, admits exactly one active or pending initialization, and rejects repeated or callback-reentrant initialization until disposal; `run()` joins the one pending initialization instead of selecting a second backend. The render scheduler publishes its container only after that exact initialization succeeds. Concurrent operations that fail on the same active backend join one runtime failover, dispose that backend once, and share the selected successor. A default retried operation quarantines every backend on which it fails and continues through the supported candidates until one succeeds or exhaustion leaves no active backend; queued work must join an already-admitted failover rather than execute on its fenced predecessor. Backend-failure observers are telemetry only: report their exceptions, but never let them interrupt fencing, disposal, or successor selection. Every `run()` revalidates both lifecycle epoch and backend identity after awaiting initial acquisition or failover, before invoking caller code and again after caller completion; stale success or failure cannot escape, record failure against, or trigger fallback in a reinitialized lifecycle. Disposal invalidates its lifecycle epoch synchronously and permits a new initialization even while the obsolete backend finishes cleanup; a backend instance returned by the older initialization must dispose itself and may never become active, publish a backend change, or trigger fallback after teardown. Backend-change publication is part of initialization settlement: recheck lifecycle currency after the callback, and roll back the installed backend when the callback throws or reentrantly disposes the host. Fire-and-forget render-scheduler invalidation and resize paths must terminate expected lifecycle-cancellation rejections during teardown and report other renderer failures.
- Renderers are projections of scene state, never the source of truth.
- `SceneRendererSnapshot` is a total projection contract. Presentation supplies an explicit nullable typed hover target and derives every per-kind selection collection from typed Scene Session targets before calling a renderer; renderers and renderer tests must not infer kind from raw IDs or omitted fields. `selectionLabelPlantIds` is a deliberately narrower projection and is non-empty only when the whole typed selection is exactly one direct Plant.
- Camera transforms go through `CameraController`; its snapshot signal owns transient viewport state and revision publication.
- The in-canvas basemap is a sibling visualization layer, not part of the renderer contract.
- Screen-space chrome such as rulers stays outside the world renderer. `canvas/rulers.ts` owns the complete ruler/scale overlay lifetime: its DOM, per-owner theme palette, latest atomic Camera snapshot, at-most-one active drag, document/window listeners, exact cursor restoration, screen-to-world guide conversion, construction rollback, and terminal idempotent teardown. Scene Chrome may compose grid/guide rendering around that owner, but must not expose ruler DOM parts or reconstruct renderer-shaped viewport adapters.
- Strokes, outlines, stack badges, labels, handles, and measurement text are readability aids. Keep their visual weight screen-readable across zoom instead of scaling them as physical design geometry.
- Selected, hover, locked-object hover, and locked-Layer hover strokes are screen-space interaction cues owned by `scene-visuals.ts`. Pixi and Canvas2D renderers should consume the same interaction visual helper so selected states remain stronger than hover and locked states do not imply editable selection.
- Canvas notice placement uses the Canvas Notice Layout seam for safe screen-space slots. Active Tool HUDs use the top-left safe slot; Location Notices use the bottom-left safe slot and reserve the scale bar before compacting.
- Use scene invalidation for content, selection, presentation, locale, theme, and hover changes.
- Use viewport invalidation for pan, zoom, and fit operations.
- Use chrome invalidation for rulers, grid, and guide-only changes.
- Do not route viewport-only work through the full scene render path.
- `renderScene()` is for scene/presentation/selection rebuilds; `setViewport()` is for camera-only updates.

## Interaction Ownership

- The factory-created `SceneInteractionSession` in `canvas/runtime/scene-interaction.ts` owns live listener lifetime, pointer capture, generic drag routing, active-tool transitions, cancellation, refresh ordering, pending host focus, and teardown. Tool adapters own only tool-specific draft and geometry state.
- Hit testing and selection geometry must stay scene-side.
- Off-canvas drag continuation, multi-drag, and additive selection behavior are part of the runtime contract.
- Species Selection is a Select-tool canvas selection gesture: double-clicking a visible editable top-level Placed Plant selects all visible editable top-level Placed Plants with the same Canonical Name across the design, excluding grouped members, locked Design Objects, hidden Layers, and locked Layers. Shift+double-click applies the same Species set additively/toggling against the current selection. The Selection Action Toolbar uses `CanvasDesignObjectSelectionModel.sameSpeciesReferenceCanonicalName` to expose Select Same Species and dispatches the non-dirty `selectSameSpecies()` command. It must not mutate Design data, Target Presentation, dirty state, or document history.
- Plant hit testing must use the same shared presentation context as renderers and fit/bounds logic.
- Interaction selection writes go through the runtime-owned selection seam.
- Editable hit testing should continue to ignore locked Layers, but hover feedback may use visible-only hit testing so locked Layers and locked Design Objects can explain why an object is blocked.
- Document-level keyboard handlers must guard with `isEditableTarget(event.target)` so Delete/Backspace/etc. do not fire while typing.

### Scene Interaction Tool Modules

- Tool modules are internal adapters behind `canvas/runtime/interaction/tool-modules.ts`; they do not change the public `SceneCanvasRuntime`, `CanvasCommandSurface`, `CanvasQuerySurface`, or `CanvasDocumentSurface` interfaces.
- The Scene Interaction Session exposes only tool synchronization, measurement refresh, transient-history operations, and idempotent disposal to `SceneCanvasRuntime`. Do not reintroduce public attach/detach, raw-handler injection, pointer-state getters/setters, or cleanup callback APIs.
- The Session installs every container/window listener, caches pointer-down bounds for the captured gesture, holds the single pointer/Space/generic-drag session, and encodes fixed event priority explicitly. `pointercancel` accepts only the matching pointer ID; matching cancellation and window blur abort active edits without history, clear hover, release Space, restore the active-tool cursor, and leave the next gesture usable. If cancellation throws before an active transaction closes, retain ownership and quarantine later pointer, drop, context-menu, wheel, and keyboard mutation routes while retrying cancellation; never let a new gesture or command overwrite retained work. Drag-over eligibility and preview are observational: use `SettledSceneReader` so pointer movement cannot resume retained maintenance; actual drops remain whole admitted commands. A rejected or throwing settled drag-over read must reset `dropEffect`, remove Plant/Saved Object preview feedback, and quarantine downstream drag-over handlers. Pointer-down and keydown admission use capture-phase listeners so pending cancellation runs before overlay buttons and global command dispatch. Pointer-move/up/cancel continuation also runs in capture phase so an owned overlay cannot strand a live gesture by stopping event propagation; when no gesture is live, those same capture handlers must return owned overlay targets to their target-phase button/HUD handlers without clearing overlay state. Preserve a valid passive Polygon draft when interruption cancels Space or middle-button panning.
- After pending cancellation gets first refusal, keydown targets inside runtime menus, toolbars, dialogs, links, buttons, or editable controls must return to normal DOM activation before the active tool sees the key. Runtime tool commands must complete the authoritative Session transition before publishing the active-tool UI mirror. Failed cancellation must leave both on the previous tool; activation or post-switch refresh failure must roll back the adapter, Session tool, settled cursor, and selection presentation before returning the failure.
- Test Scene Interaction through the user-equivalent event harness in `desktop/web/src/__tests__/support/scene-interaction-events.ts`. Broad tests should dispatch pointer, cancellation, keyboard, focus, wheel, context-menu, and drag/drop events instead of reaching into Session handlers or state.
- Shared panning, band selection, top-level Design Object dragging, snap-adjusted drag deltas, and shared gesture cleanup live in `canvas/runtime/interaction/shared-gestures.ts` behind the Session. Aborting a live Design Object drag must restore the Scene transaction and invalidate the restored scene. Band selection may keep a transient preview without reserving authority, but pointer-up completion is a whole settled command event; if another Scene Edit owns authority, quarantine that pointer-up, cancel the band, and do not write selection into the other edit's snapshot.
- Tool construction and adapter identity belong in `tool-modules.ts`; its registry must not mirror every optional adapter hook. The Session invokes the active `SceneToolAdapter` directly, owns transition/refresh/disposal order, and deduplicates shared adapter cleanup hooks.
- Session disposal detaches listeners first, cancels pending focus and active Scene Edits, then attempts every owned overlay/tool cleanup even if one fails. Runtime-owned resources use `canvas/runtime/cleanup.ts` when teardown must continue across independent owners and aggregate failures. Disposal is terminal and idempotent, and listener-installation failure must roll back listeners plus already-constructed resources before rethrowing. Collaborator factories that append DOM before returning must also remove partially acquired roots when later construction or eager refresh fails. `SceneCanvasRuntime.init()` is failure-atomic: Session-construction or initial-render failure destroys the partial Session, chrome/effects, and renderer ownership before rejecting.
- Tool modules own tool-specific state machines, including setup, per-event handling, refresh-after-viewport-change behavior, cancellation, and teardown for their own transient state.
- Shared tool context may expose only runtime-owned seams and stable projections: `SceneStateReader`, `CameraController`, Scene Edit transactions, selection setters/readers, hit testing, snapping/grid/guide reads, render invalidation, localized Species names, plant presentation context, settings commands or projections, and DOM overlay containers. Never expose the concrete `SceneStore` to a tool.
- Tool modules may create DOM overlays for tool-owned chrome, but the module that creates an overlay owns its cleanup. Session-owned overlays stay Session-owned until a later bead deliberately moves them behind a tool module.
- Creation tools must guard the target scene Layer at the mutation boundary, not only through visible UI state. Plant Stamp and drop-to-canvas use the Plants Layer, Zone drawing uses the Zones Layer, and text Annotation creation uses the Annotations Layer; hidden or locked target Layers must not create scene edits, dirty state, stale previews, or new entities.
- Selection transform affordances such as the Rotation Handle, live rotation readout, and Selection Action Toolbar should be runtime-owned DOM overlays coordinated by the Scene Interaction Session/shared selection behavior. They must not be renderer-owned projections or outer Preact chrome that reaches into runtime internals.
- `canvas/runtime/interaction/control-point-overlay.ts` owns the shared Control Point Overlay DOM, drag threshold, Scene Edit transaction, final pointer update, cancellation retry, presentation cleanup, construction rollback, and disposal lifecycle. Scene Interaction composes Control Point controllers as one collection. Zone and Measurement Guide adapters retain eligibility, geometry derivation and constraints, draft writes, equality, labels, and optional live measurement presentation; do not copy the lifecycle back into either adapter or widen this seam to Rotation Handle semantics.
- The Selection Action Toolbar and Rotation Handle are Select-tool-only selection affordances. Hide them for Annotation Text, Zone drawing, Plant Stamp, Object Stamp, Plant Spacing, Hand, inline Annotation editing, Canvas Context Menu interaction, active Design Object drags, active rotation drags, active Zone Control Point drags, and future non-select tool interactions.
- Single-selection Rotation Handle drags are Scene Edit transactions owned by the runtime overlay. They must update rotation live, commit one history entry only past the no-op angle threshold, and abort cleanly on Escape, dispose, tool transition, or tiny/no-op pointer-up.
- Multi-selection and Object Group Rotation Handle drags use one pivot from combined visual bounds. Placed Plants rotate by moving positions without adding visible plant orientation; Rectangular/Elliptical Zones move their centers and increment orientation; Linear/Polygonal Zones rotate points; Object Groups mutate member geometry and group position without using hidden group rotation.
- The Selection Action Toolbar reads `CanvasQuerySurface.getDesignObjectSelection()` through the interaction deps, dispatches mutations through a narrow `CanvasSceneEditCommandSurface` role, and must not steal focus when it appears. Icon-only buttons may expose custom tooltips and focused keyboard activation, but hover/highlight alone must not make keyboard activation implicit. Action tooltips open above the toolbar, and the toolbar stacking layer must stay above the Rotation Handle so hover labels remain readable.
- Selection Action Toolbar placement must not block direct manipulation affordances such as Zone Control Points. Prefer non-overlapping placement; if no clear placement exists, hide or offset the toolbar instead of requiring a user-managed close button. Clear all toolbar button tooltip state whenever the toolbar hides, contents change, selection changes, active tool changes, disposal runs, or a transient interaction starts.
- The Selection Action Toolbar should show Group for any editable top-level multi-selection whose flattened typed concrete member set has at least two members and whose member Layers are all visible and unlocked, including mixed Placed Plant, Zone, Annotation, and Object Group selections. It should show Ungroup for selected editable Object Groups.
- Group/Ungroup shortcuts, menus, and toolbar actions must dispatch through the same Scene Edit command behavior. The command may no-op for ineligible selections, but toolbar availability and command eligibility should be derived from the same flattened typed member rules.
- Select-tool Design Object drags hide the Selection Action Toolbar and Rotation Handle from drag start until the drag ends or is canceled. Restore them only after the scene/selection refresh so their placement reflects the final geometry. During active drags, clear passive hover presentation such as Hover Tooltip, hovered entity projection, and locked-object affordance; do not force Hover Tooltip to reappear on pointer-up.
- Pointer-up, interruption, and Control Point commit/cancel paths must restore drag presentation and run their final measurement refresh even when rendering or another cleanup reports an error. Keep each active drag transaction reachable until commit or abort finishes. A commit failure before history accepts the transaction may cancel back to its captured snapshot; a later publication failure must retry the already-recorded committed outcome.
- Runtime-owned overlays such as the Selection Action Toolbar, Rotation Handle, locked-object affordance, and tool HUDs may suppress passive hover/preview pointermoves, but active pointer gestures must receive movement before overlay ignore checks so drags, pans, and rotations do not freeze while crossing overlays.
- Selection Action Toolbar command availability should be derived from the selection read model. Keep mutation and dirty/undo behavior behind the command surface and Scene Edit transactions rather than duplicating it in overlay code.
- Scene Interaction Session owns canvas-scoped browser-default suppression such as `contextmenu`; handlers should preserve native behavior for editable targets and non-canvas panels.
- Pointer, context-menu, and drop events that encounter a retryable pending authority settlement may finish that settlement, but must quarantine the triggering event whether recovery returns busy or throws. Never continue hit testing, selection, dragging, menu presentation, or placement from an event whose initial Scene assumptions were made while unsettled.
- The Canvas Context Menu is a runtime-owned canvas overlay coordinated by the Scene Interaction Session. It dispatches Copy, Paste, and Delete through canvas scene-edit command surfaces, keeps unavailable commands visible but disabled, and must place itself dynamically inside the visible canvas so app chrome or viewport edges do not hide it. Hiding or replacing a document clears the menu's captured world point and focus so a stale button cannot act on the next Scene.
- Canvas Context Menu target selection follows design-tool behavior: right-clicking an unselected editable top-level Design Object selects only that object; right-clicking a Design Object already inside the current selection preserves the current selection; right-clicking empty canvas preserves selection and uses the clicked canvas point for context paste. Right-clicking a directly locked Design Object may select it as a locked target, but Copy and Delete remain disabled because locked objects are not editable mutation sources. Right-clicking a visible but structurally blocked Design Object, such as a locked-Layer object or Object Group blocked by a locked member, must not leave an unrelated editable selection active for Copy/Delete; those commands stay disabled for that menu context. Resolve the topmost visible hit before falling back to editable-only hit testing so a blocked object over an editable object does not retarget the menu to the object behind it. Copy and Delete availability requires an editable selection with no locked or blocked targets; mixed editable plus locked/blocked selections keep those commands visible but disabled.
- Do not open or retarget the Canvas Context Menu while a direct-manipulation Scene Edit is live. Suppress that context-menu event without canceling the active edit so its existing pointer continuation can still commit or abort normally.
- `canvas/runtime/scene-runtime/arrangement-placement.ts` is the shared authority for placing copied Scene arrangements. Clipboard Paste/Paste At/Duplicate, Object Stamp, and Saved Object Stamp must adapt their source into a `SceneArrangementTemplate` and delegate fresh identity allocation, cross-kind identity and Zone-name collision handling, ellipse translation, typed Object Group remapping, atomic insertion, and canonical top-level selection to that transaction-owning seam.
- Arrangement source policy stays with its caller: clipboard capture and pin behavior, Object Stamp sampling/revalidation/repeated preview, and Saved Object Stamp decoding/Layer eligibility/one-shot ghost lifecycle do not belong in the placement seam. Keep explicit source identities in adapters, especially where a Saved Object Stamp Zone payload ID differs from its user-visible Zone name.
- Normal Paste and Duplicate use the internal canvas clipboard and place copies one meter to the right of the copied Design Object selection; repeated normal Paste or Duplicate advances one additional meter to the right each time. Context Paste from the Canvas Context Menu recomputes the copied payload's combined visual-bounds center with the current viewport and presentation context at paste time, then translates the copied Design Objects so that center lands on the clicked canvas point, preserving relative geometry and group membership.
- Saved Object Stamp placement should follow Paste/Object Stamp transaction semantics: one drop or click placement is one Scene Edit history entry, undo removes all objects created by that placement, and the final selection contains the newly placed top-level Design Objects. Placement does not create a new Object Group just because the stamp contains multiple objects, but Object Groups captured inside the stamp remain Object Groups with remapped members.
- Saved Object Stamp capture is a whole settled Scene command because it reads selection and persists an external library entry. Favorites UI and context actions dispatch `saveSelectionAsObjectStamp()` through the command surface; they must not call the workbench capture method directly. Native drag feedback and drop placement also cross command admission, and a rejected busy drop must preserve its source for retry.
- Saved Object Stamp placement anchors the stamp's combined visual-bounds center at the pointer/drop point and preserves relative geometry, size, spacing, orientation, colors, symbols, annotation text, Zone geometry, and captured group names. Do not auto-scale, rotate to cursor, substitute Species, or depend on Species Catalog availability for placement.
- Saved Object Stamp placement is all-or-nothing across target Layers. If any object type in the stamp requires a hidden or locked target Layer, block placement with feedback instead of creating a partial arrangement. Placed copies are unlocked even when directly locked source objects were captured into the saved stamp.
- Saved Object Stamp save eligibility may include directly locked selected Design Objects, but must still reject structural blockers such as hidden Layers, locked Layers, missing targets, and grouped members selected outside their Object Group. Grouped members should be saved through their selected Object Group instead.
- Saved Object Stamp payloads should preserve visible arrangement data only. Strip lock state, plant notes, planted dates, plant quantities, Zone notes, and other non-visual planning data unless a field is required to avoid invalid Design Objects or broken group membership.
- Saved Object Stamp drag/drop and armed placement should show full ghost copies of the arranged plants, zones, annotations, and captured groups in their real relative positions, using translucent non-interactive styling. Exact placement ghosts belong to the canvas runtime; framed recognition thumbnails for library inspection belong to the Favorites panel. If target Layers block placement, do not show the arrangement ghost; surface blocked feedback instead. The placement preview should update with snapping and disappear on placement, cancellation, tool switch, document replacement, or runtime disposal.
- Scene edits from tools must go through `SceneEditCoordinator`; modules must not mutate persisted scene arrays outside the Settled Scene Authority except for explicitly transient draft state owned by the runtime. Non-tool commands that read Scene state before writing must also cross the authority's caller-shaped settled-command admission for the whole operation.
- Drag-to-create Line, Rectangle, Ellipse, and Measurement Guide tools own a long-lived Scene Edit from accepted pointer-down through pointer-up commit or cancellation. This prevents an unrelated immediate command from consuming pointer-up recovery. Keep the draft transaction reachable until commit/abort settles and report it through the tool adapter's active-edit query. Use `onCommitted` for fallible, idempotent overlay cleanup, but clear the retained draft handle only after `commit()` returns: the authority still performs a fallible post-continuation backfill drain before release, and a quarantined later event must be able to resume it through the same handle.
- Tool modules must treat `setTool()` changes, `dispose()`, document replacement, Escape cancellation, source invalidation, and viewport refresh as lifecycle events that leave no stale overlays, listeners, selections, previews, or pointer capture behind.
- Do not install module-level `effect()` or global listeners from a tool module unless that module also owns an explicit disposer and `import.meta.hot.dispose()` cleanup. Prefer Session-dispatched events for canvas tool input.
- Current Scene Edit tool adapters include Annotation Text, Zone drawing, Object Stamp, Plant Stamp, and Plant Spacing. Do not reintroduce tool-specific fields, source state, preview state, or direct tool branches into the Scene Interaction Session; route through a `SceneToolAdapter` and keep the state machine in the tool module.
- `canvas/plant-stamp-source.ts` owns Plant Stamp Source selection plus drag data parsing/serialization. Plant Stamp tool modules consume that seam and clear the selected source on adapter deactivation/disposal; Species Catalog UI modules call it instead of writing source state or hand-assembling drag payloads.
- Guard Scene Edit ownership with source boundary tests in `scene-interaction-tool-boundary.test.ts`, focused tool-module tests such as `plant-spacing-tool.test.ts`, and user-equivalent Session lifecycle tests in `scene-interaction.test.ts`. Add tests before moving another tool concern across the Session/adapter boundary.

## Zone Geometry

- Zone file data carries orientation as `Zone.rotation`; scene runtime entities expose it as `SceneZoneEntity.rotationDeg`. Missing rotation from older documents hydrates to `0`, and new saves serialize an explicit numeric rotation.
- Rectangular and Elliptical Zones are oriented shapes. Rendering, hit testing, band selection, object stamp previews, selection/group bounds, zoom-to-fit, and Zone Measurements must consume the shared helpers in `canvas/runtime/zone-geometry.ts` instead of duplicating axis-aligned bounds.
- Single-click Zone selection uses boundary-proximity hit testing, not filled-area hit testing. Rectangular, Polygonal, and Elliptical Zones should be selected only when the pointer is on or near their Zone boundary; Linear Zones continue to use near-line hit testing. Keep the tolerance screen-space stable, around 6 px, so interior clicks pass through to contained Design Objects or clear selection when nothing else is hit.
- Zone boundary hits must remain lower priority than direct hits on visible contained Design Objects such as Placed Plants or Annotations. When the pointer is over both a Placed Plant and a Zone boundary, selection and drag intent should target the Placed Plant, not the Zone.
- Zone hover presentation should mirror boundary-proximity selection rules. Hover the Zone boundary, not the filled interior; if a Placed Plant or Annotation is also under the pointer, show that Design Object's hover cue instead. Locked or blocked Zone boundary hover may explain blocked state through existing locked affordance rules but must not imply editability.
- Whole-Zone dragging should start from a boundary-proximity Zone hit, not from the filled interior. For selected Zones, interior clicks and drags should still pass through to contained Design Objects or behave as empty canvas when nothing else is hit; Zone Control Points handle reshaping, and Zone boundary drag handles moving.
- Band selection remains area/intersection-based for Zones. Do not use boundary-only logic for selection rectangles.
- Elliptical Zones store `points[0]` as the world center and `points[1]` as the radius vector. Translation-style edits, Paste, Duplicate, and Object Stamp must offset only the center while preserving the radius vector; linear, polygonal, and rectangular Zones continue to translate their world points.
- Linear and Polygonal Zones still derive geometry from their saved points; keep their rotation value at `0` unless a future feature deliberately defines oriented behavior for those Zone types.
- Zone Control Points are screen-space editing affordances for reshaping a single selected editable top-level Zone in the Select tool. Do not show them for multi-selection, Object Group selection, locked Zones, locked Layers, hidden Layers, grouped members, or any structurally blocked selection.
- Zone Control Points should be visible immediately for an eligible selected Zone but remain visually quiet: small, readable, screen-space sized, unlabeled by default, and emphasized only on hover/focus or active drag. They are not persisted Design Objects and must not reduce normal canvas readability.
- Zone Control Points must be pointer-input friendly across mouse, pen, and touch. Keep visible marks visually quiet while using a larger invisible hit target, and suppress browser gestures only for the active Zone Control Point drag path.
- Zone Control Points have pointer priority only for the current single selected editable Zone and only inside their explicit hit target. This priority may beat an overlapping Placed Plant or Annotation so the selected Zone remains reshapeable; outside active Control Point hit targets, direct Design Object hits stay higher priority than Zone boundary hits.
- Zone Control Point identity follows Zone type: Linear Zones expose endpoints; Polygonal Zones expose vertices; Rectangular Zones expose corners while preserving rectangular geometry; Elliptical Zones expose four oriented cardinal radius handles instead of sampled perimeter points.
- Rectangular Zone Control Point drags preserve the Rectangular Zone type. Dragging a corner resizes from the opposite corner in the Zone's local rotated space, keeps rotation as a Rotation Handle concern, and clamps or rejects degenerate width/height rather than converting to a free Polygonal Zone.
- Elliptical Zone Control Point drags preserve the Elliptical Zone type. East/west handles change width, north/south handles change height, the opposite local side stays anchored, center and radius vector are derived from the dragged and anchored sides, and rotation remains a Rotation Handle concern.
- Zone Control Point drags use existing grid and guide snapping rules for live preview and commit. Begin one Scene Edit transaction on pointerdown, mutate live during pointermove, commit one undoable edit on pointerup only when geometry changed, and abort cleanly on Escape, tool switch, document replacement, Session disposal, or tiny/no-op drags.
- Active Zone Control Point drags hide Selection Action Toolbar, Rotation Handle, passive Hover Tooltip, hovered entity projection, and locked-object affordances until the drag commits or cancels. Restore selection-dependent overlays only after the scene and selection-dependent measurements refresh.
- Zone Control Points stack above Zone Measurements and own pointer input priority. Zone Measurement overlays are read-only and must remain pointer-transparent so they cannot block Zone Control Point drags.
- Existing Polygonal Zone editing starts by dragging existing Zone Control Points only. Adding or deleting Polygonal Zone vertices is separate future behavior with its own gesture, undo, and selection rules.

## Zone Measurements

- Zone Measurements are derived presentation for zone geometry, not persisted design objects or annotations.
- Render Zone Measurement labels through a renderer-independent, screen-space overlay so Pixi and Canvas2D backends do not duplicate measurement UI.
- Show Zone Measurements while drawing a zone and for a single selected top-level Zone; suppress them for multi-selection and group selection.
- Zone Measurements update live during Zone Control Point drags and describe the snapped preview geometry, not only the final committed Zone geometry.
- Linear Zones use one edge-length measurement and never show area, width, or height labels.
- Rectangular and polygonal Zones use the same measurement model: horizontal edge-length labels at readable edge midpoints plus one area label for the whole Zone.
- Elliptical Zones use width, height, and area measurements because they have no Zone Edges.
- Linear Zone drawing uses drag-to-endpoint interaction. The snapped drag start and current pointer define the saved endpoints.
- Elliptical Zone drawing uses drag-to-bounding-box interaction. The snapped drag start and current pointer define opposite box corners; the stored geometry derives center and radii from that normalized box.
- Polygonal Zone drawing uses click-to-place vertices. The Zone Draft remains transient until the user closes it; Escape cancels the draft, Backspace removes the last vertex, Undo/Redo step through draft vertices before committed Scene Edit history, and closing creates one scene edit.
- During polygonal Zone drawing, live measurements include committed edge lengths, the active edge to the pointer, and once the preview has enough points to form a polygon, the closing edge back to the first point plus live area.
- Format Zone Measurement values compactly: centimeters for distances below one meter, meters for ordinary distances, square centimeters for areas below one square meter, square meters for ordinary areas, and hectares for large areas.
- Live Zone Measurements must describe the geometry that will actually be created, including snap-to-grid and snap-to-guides effects while drawing.
- Hide edge-length labels for edges that are too short in screen space; zooming in should make them available again. Area labels should remain visible for valid areas.
- Zone Measurement overlays respect Zone layer visibility. Locking does not suppress read-only measurements for an otherwise visible selected Zone.

## Annotation Rules

- Annotation geometry comes from shared helpers in `runtime/annotation-layout.ts`.
- Annotation text is readable presentation anchored to a design position; its visible text bounds are screen-space and should not be treated as physical world geometry.
- Annotation `rotationDeg` is visible geometry. Renderers, hit testing, band selection, grouping, zoom-to-fit, and selection/read-model bounds must consume the rotated frame helpers from `annotation-layout.ts` rather than reading unrotated text bounds directly.
- Use the same annotation bounds for hit testing, band select, grouping, zoom-to-fit, and selection outlines.
- Visible text should win hit testing over underlying zones/plants when it is on top.
- Creating a new text Annotation is owned by `interaction/text-annotation-tool.ts`; editing an existing text Annotation in place is owned by `interaction/annotation-inline-editor.ts` and is started from Select-tool interaction only after the Design Object selection read model says the target is editable.
- Existing text Annotation inline editing should use a runtime-owned cross-platform double-click recognizer instead of relying only on `PointerEvent.detail`. The recognizer should require the Select tool, primary button, the same editable Annotation target, bounded pointer movement, and a short click interval; additive selection gestures and active drags should not enter inline editing. Starting inline editing selects the Annotation first, then hides Selection Action Toolbar and Rotation Handle until the edit commits or cancels.

## Plant Presentation

- Plant geometry, color, Plant Symbol resolution, and stack badges come from `runtime/plant-presentation.ts`.
- Visual Footprint is the shared presentation boundary for symbolic plant sizing; rendering, hit testing, band select, grouping bounds, and zoom-to-fit must consume the same computed footprint. Additional click/touch padding is allowed only as an explicit interaction affordance.
- Selected Plant presentation contexts, including Plant Color and Plant Symbol popover contexts, should prefer the active localized Common Name from the runtime presentation cache and fall back to the persisted Placed Plant Common Name only when no localized name is available.
- Plant Symbols are built-in marker recipes visually composed within the existing circular Visual Footprint. The Visual Footprint is the interaction and layout boundary; glyph recipes should be optically balanced and may be inset instead of filling that boundary edge-to-edge. They must not change hit testing, band select, grouping bounds, zoom-to-fit bounds, stack badge placement, hover rings, selection rings, or locked-state rings.
- Render Plant Symbols with Pixi and Canvas2D native primitives rather than runtime SVG parsing, image textures, DOM overlays, or a new icon dependency. At low zoom where the symbol is not readable, collapse all symbols to the same dot/round marker.
- Plant Symbols apply inside the symbolic marker Visual Footprint. Do not reintroduce a separate canopy-spread display mode or other plant-size display modes without a new decision record.
- Plant Symbols stay upright and ignore placed plant rotation unless a future feature deliberately defines oriented marker symbols.
- Stack badges indicate placed plants whose centers collapse to nearly the same screen position, not all plants whose symbolic Visual Footprints overlap. Badge placement should derive from the current plant visual radius instead of fixed legacy dot-size assumptions.
- Do not reintroduce general per-plant labels on canvas. Identification is through hover tooltip and single-plant Selection Labels, except for explicit Pinned Plant Names, which are persisted per placed plant and render as always-visible labels.
- Hover tooltip is an HTML overlay managed by the Scene Interaction Session through `runtime/interaction/hover-tooltip.ts`.
- Hover species highlight flows through renderer snapshots.
- Selection Labels are temporary identification for the current selection only. Render them only when the whole canvas selection is exactly one unpinned Placed Plant; multi-selection, mixed-object selection, and selected pinned plants should not render transient plant names.
- Placed Plant markers are symbolic position markers. Size them with a smooth absolute-scale Visual Footprint curve with soft screen-readable limits, not a hard pixel cap, design-reference-relative sizing, or species canopy-spread geometry. Target about 2px minimum, about 6.75px asymptotic maximum, and gradual growth from roughly 2.2px at 1 px/m to roughly 6.3px at 200 px/m.
- Do not reintroduce general Display by or Color by plant presentation controls without a new decision record. Manual Plant Color and per-species Plant Color remain supported.
- Species-cache backfill may enrich metadata, but production geometry should not depend on ad hoc empty-cache fallbacks.

## Target Projection

- Timeline, budget, and consortium identity uses typed `PanelTarget[]` / `PanelTarget` wire values, but frontend callers should import Target helpers from `desktop/web/src/target/`.
- Do not reintroduce string matching against timeline descriptions, legacy `plants` arrays, budget descriptions, or consortium canonical-name fields.
- Use the Target resolution helpers to map typed targets to scene plant/zone IDs for canvas highlights.
- `manual` and `none` targets intentionally resolve to empty sets and are not errors.
- Panel-origin hover and selection are presentation inputs. Resolving them must not mutate real canvas selection, labels, dirty state, or history.
- App-owned Target presentation state is exposed through `app/panel-targets/presentation.ts`; runtime adapters and map surface controllers should consume that seam instead of raw `app/panel-targets/state.ts` signals.
- Target map overlays for Zones must project effective Zone geometry. Rectangular and Elliptical Zone targets use the rotation-aware helpers in `canvas/runtime/zone-geometry.ts`; do not project raw persisted points for oriented Zone types.
- Canvas-origin hover remains separate.

## Canvas2D Tab Components

- Use `useCanvasRenderer` for DPR-aware canvas setup, resize observation, and redraw lifecycle.
- Use shared renderer utilities rather than duplicating drawing helpers.
- Planning tab read-models such as Timeline Action rows, layout lanes, species picker options, and Target derivation belong in `app/planning-projection/`; Canvas2D renderers own drawing, row heights, hit geometry, and pointer math.
- Planning tab UI should consume the relevant workbench or Planning Projection surface instead of calling canvas query surfaces directly for placed plants, localized Species names, Design Session planning arrays, or planning Target presentation. Budget UI uses the Budget Item Workbench; Timeline UI uses the Timeline Action Workbench; Consortium UI uses its Planning Projection/workbench seam.
- Planning Canvas Interaction Frame (`app/planning-canvas/interaction-frame.ts`) owns shared planning-canvas document listener lifetime, active drag finish/abort ordering, hover/selection Target Presentation write ordering, stale visible-item cleanup, and disposal cleanup. It is separate from the Scene Interaction Session in `canvas/runtime/scene-interaction.ts`. Planning surface adapters keep geometry, rendering, local presentation state, and Design Edit semantics in their own modules while supplying drag lifecycle delegates to the planning frame.
- Timeline Action document editing belongs in `app/design-edit/`; Timeline drag math belongs in `app/timeline/interaction.ts`; Timeline Action Canvas behavior belongs behind `app/timeline/canvas/`. `geometry.ts` owns row offsets, canvas height, action bounds, sidebar/ruler dimensions, and action-type row lookup. `host-model.ts` owns Planning Projection input assembly, canvas sizing, render callback/dependency assembly, frame listener installation, and tooltip/popover host-model projection. `controller.ts` owns Preact-facing interaction state, geometry/render-state refs, frame delegate assembly, and the small frame adapter seam. `interaction-frame.ts` owns pan, move, resize, autoscroll, origin freeze/restore, pointer ordering, wheel handling, popover open/save/delete/cancel ordering, keyboard delete, Timeline-specific drag cleanup delegates, and composition of the Planning Canvas Interaction Frame for listener lifetime, active drag finish/abort ordering, stale identity cleanup, and Target Presentation writes. Canvas2D timeline UI consumes `useTimelineActionCanvasHostModel()` from `app/timeline/canvas/` plus the shared Canvas2D renderer hook; it must not import Timeline renderers, settings signals, interaction ordering, planning input assembly, or Design Edit transaction internals directly.
- Test Timeline Action Canvas interaction-frame behavior through `timeline-interaction-frame.test.ts`; keep dependency and ownership rules in `frontend-architecture-policies.test.ts`.
- Consortium drag math belongs in `app/consortium/interaction.ts`; Consortium canvas event ordering, hover bridge behavior, drag lifecycle delegates, and the Planning Canvas Interaction Frame adapter belong in `app/consortium/workbench.ts`. Shared planning document listeners, active drag finish/abort ordering, stale hover cleanup, and Target Presentation writes should go through the frame. Canvas2D consortium UI owns layout/render wiring, not interaction ordering or Design Edit transaction internals.
- Consortium Succession Phase and Stratum facts belong in `app/consortium/time-model.ts`; Canvas2D consortium renderers own pixel layout and drawing, not the Consortium Time Model.
- Canvas2D renderer functions receive `t` for i18n.
- Cache row offsets and layout computation in refs/memos for pointer paths.
- Snapshot drag-start values that can change mid-drag.
- Track whether a drag actually mutated state before marking documents dirty.
