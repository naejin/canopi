# Raster rework completion receipt

Status: evidence — initialized handoff structure; no C0–C5 execution or capacity result is claimed yet.
Tracking: `canopi-j571`; executor records the completion bead here after claiming it. bd owns progress.
Current guidance: [prompt](completion-agent-prompt.md), [contract](completion-design.md), [previous receipt](ordered-cog-receipt.md), [debrief](review-and-debrief.md#whole-rework-delivery-and-improvement).

## Baseline and claim boundary

Accepted correction: `34e4ded4` on `feature/bounded-raster-generations`; independent scope and checks are in the [review](ordered-cog-review.md#accepted-correction-at-34e4ded4). No full-workbench, larger-capacity, integration or platform claim follows from that acceptance.

Replace this initialization with actual starting/integrated/candidate revisions and the preserved user-work inventory during C0. Do not infer the handoff commit hash from a prose placeholder; obtain it from Git. Record which remote tips were verified.

## Capability and acceptance evidence

Maintain one compact table mapping C0–C5 to implementation revision, actual observed workflow, decisive test/fixture or live observation, result and remaining limitation. An unavailable gate has its exact prerequisite and affected claim. Keep production policy separate from test-only overrides. Retain earlier results with their original revision instead of copying their counts into the new candidate.

## Capacity and scientific measurements

Record fixture identity/hash, dimensions/layout/NoData/units, real versus synthetic provenance; host/RAM/OS/WebView/engine identity; idle baseline and aggregation/sampling method; simultaneous incremental RSS/cache/queue peaks; temporary/durable disk; cancellation phase/settlement; cold and three warm display observations. Include numeric plane/mask results and exact production admission after the gate. Values not measured remain unknown. Raw private rasters, credentials and giant transcripts stay out of Git.

## Gates, application and platform evidence

Record exact command, working directory, revision, result and useful log/artifact pointer. Separate Linux/Windows/macOS compiler, native caller, packaged window and external-fixture results. Separate gallery, browser and actual Web package evidence. Mounted-map claims identify confirmed placement, visible states and corresponding source/result generations. Partial screenshots or source-list order alone cannot prove a settled full rendering.

## Failures and improvements

For material events, record: revision and triggering case; accepted invariant; design omission / implementation deviation / test-oracle gap / reviewer oversight / environment-tool limitation (allow overlap); detector; repair; failing and healthy evidence; pre-delivery versus independent discovery. Consolidate repeated events instead of appending a diary.

For each improvement actually tried, record smallest change, evidence from its next actual use, keep/revise/drop and owner/follow-up. Name untested proposals explicitly. If the old instructions already covered the behavior, do not claim another instruction solved it. Timing/cost/courier counts are recorded only when directly observed, with scope; otherwise unknown.

## Final disposition and delivery

Implementer supplies completed and pending outcomes, integrated accepted stack versus new candidate, actual admission limits, remaining external prerequisites, docs/tool changes, bd reconciliation, remote verification and preserved user files. The main agent adds a revision-specific independent disposition and escaped findings. Integration of new work and release are recorded only after they actually occur with authorization. Link the final debrief synthesis rather than duplicating it here.
