# Repair ordered COG delivery through the real Desktop workflow

Status: active — sole current raster implementation assignment; repairs the partial delivery at `783e31e3`.
Tracking: `canopi-jv8a.4`; reconcile `canopi-kko3`; parent `canopi-jv8a`, epic `canopi-j571`.
Current guidance: [fixed design](ordered-cog-design.md), [independent findings](ordered-cog-review.md), [collaboration protocol](collaboration-protocol.md), [LiDAR](../../agent/lidar.md), [debrief](review-and-debrief.md#ordered-cog-delivery-and-final-debrief), [delivery](../../workflow/delivery.md).

## Outcome and authority

Complete the ordered COG workflow already delivered in part: add independent sources, reorder/remove, Undo/Restore, display and recompute slope with consistent topmost-valid samples and exact historical compatibility. Repair all eleven finding groups in the independent review and adjacent failures in these same required workflows. Keep the existing architecture. This is a correction, not a restart or an alternatives study.

You own diagnosis, implementation, meaningful regressions, routine interface details, UI wiring, final verification, documentation reconciliation and self-review. Work through the whole assignment and return one consolidated delivery. A long run, context boundary, failing test or completed backend phase is not a reason to request another continuation prompt. Record resumable checkpoints in bd and continue. Discoverable repository facts and ordinary technical decisions are yours to resolve.

Escalate only a demonstrated conflict with the fixed product/scientific/compatibility/resource contract, destructive data handling, a new dependency/engine, or required external authority. Give evidence, your recommendation and the precise blocked work; continue independent work. No delegation, integration, release or branch deletion is authorized.

## Start and preserve

1. Read repository instructions and the linked review/design, especially [the repair decisions](ordered-cog-design.md#8-repair-contract-after-review-of-783e31e3). Read relevant subsystem guides as needed; historical prompts do not expand scope.
2. Check status in both checkouts. Continue `feature/bounded-raster-generations` from `783e31e3`, preserving its merged documentation, accepted `a5fc7d7b`, C1/C2 and ordered-COG commits. Do not start from main or the old `0696bd3d` baseline. Inspect and claim/re-scope `canopi-jv8a.4`; keep `canopi-kko3` resolution tied to caller evidence.
3. Incorporate the forwarded documentation commit. Keep this current prompt/design/review, while preserving newer branch-local receipts, LiDAR/build-release inventories, historical product-closure outcome and ordered delivery outcome. Correct their current claims against the review; do not erase revision-labelled evidence. The primary checkout's dirty `desktop/src/native_operation.rs` and `.beads.gate.lock` are user-owned: do not read, stash, stage or reset them.
4. Establish focused failing regressions at the real boundaries. The optional ignored diagnostic archive is a shortcut only; the committed review gives enough inputs to recreate every case. Use the repository's pinned toolchain in any isolated test checkout. Preserve unrelated changes.

## Work in dependency order; continue automatically

**Restore trustworthy reads and numeric publication.** Fix the public collection-read lock recursion, topmost-valid measuring semantics, whole-batch admission, common first-batch CRS, zero-valid rejection, cumulative extent, source-region coordinates and legacy-head/sparse compatibility. Keep source priority separate from legacy role interpretation. Test the actual published sample/statistics pair, not just a helper.

**Finish publication, history and dependent settlement.** Implement the settled command, Undo metadata/migration and no-op rules in design §8. Propagate cancellation through compatibility reads. Exercise legacy-only slope, superseded jobs, failure/cancel/restart and empty composition; a stale result cannot be Ready. Preserve committed success even when post-commit cleanup or refresh scheduling reports a diagnostic.

**Complete the existing read/UI seams.** Bound incremental member/history reads and intersecting read candidates using existing indexes. Await real edit completion; fence late selection/head responses; keep errors visible; refresh open History, sources and map. Show actual source filenames and version identity, remove retired overlap counters, and retain the current dock. Exercise edits at page boundaries and loading/pending states. Choose private helper names, typed field names and local factoring yourself.

**Verify the whole outcome and simplify.** Review the final diff separately from implementation, following each changed public caller through settlement/reopen/presentation. Remove redundant new-write paths when tests establish they are unused; retain needed historical readers. Ask of each new abstraction: which current caller needs it, and why is the existing owner insufficient? Fix concrete in-scope discoveries before forwarding. Do not use this question to trigger a general refactor.

These are internal dependency phases, not approval gates. All eleven findings remain required; grouping them is intended to reduce repeated edits and full-suite runs.

## Verification and evidence

Use the review's counterexamples and healthy controls as the regression map. Include actual public reads, backend Apply of a rejected batch, published statistics versus reopened samples, sparse/overlay legacy reads, settlement outcomes, repeated Undo to exhaustion, equal-composition Restore, paging, deferred UI responses and source/result identity after restart. A local fixture assertion does not establish the full caller outcome. Reuse small synthetic GeoTIFFs and existing helpers; no external dataset is needed to establish these regressions.

Run focused checks while changing code, then repository-required final Rust, native-command, frontend, generated-contract, edition and documentation gates applicable to the diff. Record exact commands, revision and counts; distinguish full workspace tests from the ignored LiDAR route and unavailable external IGN fixture. Do not rerun unchanged code for prose-only edits. Rebase/preserve merges and rerun affected gates as required by the delivery guide before pushing.

Drive the real Desktop add → reorder → remove → repeated Undo → Restore → slope → restart workflow in a fresh isolated profile using existing edition guidance. Include keeping History open, changing selection during a pending read, and empty/legacy-only states. Inspect available display/session/port isolation before declaring a blocker; an absent convenience tool alone does not establish that all existing routes are unavailable. Preserve the user's app, servers and data; do not install dependencies or build a new GUI harness just to satisfy this task. If safe isolation cannot be established, record the actual missing prerequisite, exact runnable steps and remaining acceptance risk. Automated/component evidence is useful but is not a claimed live-window pass.

## Improve while delivering

Use bd for progress and one existing `ordered-cog-receipt.md` for measured evidence; no additional tracker or transcript collection. Keep a compact finding-to-regression/disposition map in that receipt. Record discoveries before/after delivery and tool failures when observed, not reconstructed at the end. The [debrief](review-and-debrief.md#ordered-cog-delivery-and-final-debrief) defines the final outcome and adoption fields.

You may repair small, reversible task-local scripts/fixtures/diagnostics using existing dependencies when an observed failure blocks trustworthy delivery. Verify the failing case and healthy use, update the narrow operating guide, then return to product work. Do not modify installed skills, add a reporting framework, rewrite shared workflow, perform a capacity campaign or pursue speculative optimization. Broader proposals become evidence-backed follow-up beads. `canopi-jv8a.2` preview-range semantics remain separate from the source-region bug here.

Prefer a decisive caller regression or removing obsolete code over another layer of instructions. For each actual improvement record failure → smallest intervention → observed result → keep/revise/drop → next-use check and owner. Unknown cost/time stays unknown. Do not infer model productivity from suite size, document size or the user's cost motivation.

## Consolidated handoff

Reconcile operating guides, design/ADR status and README routes without marking independent acceptance. Update the existing debrief with one compact implementation outcome, preserving historical outcomes. Export intended bd changes, commit and push the existing branch under repository hygiene. Leave `canopi-jv8a.4` open for independent disposition; `canopi-kko3` needs recomputation and complete settlement evidence, not merely the chosen decision.

Return final commit/branch and receipt; demonstrated user capabilities; each finding's regression/disposition; exact final gates and unavailable evidence; necessary retained complexity and removed machinery; improvements applied versus untested proposals; remaining decisions/follow-up beads; and user-owned files left untouched. Main-agent review and the user's integration decision follow this delivery. Finish all preceding authorized work without another routine permission round.
