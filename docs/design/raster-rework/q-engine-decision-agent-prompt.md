# Raster qualification — artifact decision brief

Status: retired — delivered at `71abad0f`; superseded by the user's GeoLibre adoption decision.
Tracking: `canopi-kqpp`, parent `canopi-j571`; Q remains unqualified.
Current guidance: [native integration assignment](geolibre-integration-agent-prompt.md), [settled design](geolibre-integration-design.md), [LiDAR](../../agent/lidar.md).

The read-only brief is retained in the [historical receipt](q-typescript-receipt.md#artifact-decision-brief-after-qualification-reset). Original execution instructions are recoverable in Git at `71abad0f`; they authorize no new investigation, build or experiment.

The measured npm build did not match the declared source pin. The user subsequently selected useful GeoLibre components and native production integration. The new design explicitly builds the GeoLibre-selected native core from source, retains GDAL where useful and preserves the old correspondence failure as history. Do not repeat the artifact-selection loop.
