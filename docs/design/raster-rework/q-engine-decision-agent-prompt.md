# Raster qualification — artifact decision brief

Status: proposed — sole next assignment, executable when forwarded by the user.
Tracking: `canopi-kqpp`, parent `canopi-j571`; leave Q open and unqualified.
Current guidance: [approved reset](qualification-reset.md), [main plan](../raster-data-analysis-rework.md), [collaboration protocol](collaboration-protocol.md), [LiDAR guide](../../agent/lidar.md).

## Mandate

Stop the lifecycle/harness repair assignment. Deliver the evidence needed for the user to choose the next engine/artifact action. This is a bounded, read-only investigation with a short documentation output—not permission to implement a replacement harness, fix L1–L3, run another pilot or design the whole remaining project. The main agent owns architecture/recommendation; you establish facts; the user remains courier and engine authority.

Inspected baseline is `a162c1a0`, implementation `f9b5c10c`, bead export `4564acc7`; inspect newer changes before proceeding. Preserve the feature stack, saved pilot evidence, accepted numeric/evaluator fixes, import-map migration and user-owned `desktop/src/native_operation.rs` edit. If work on the superseded prompt has started, report and preserve it without reverting or continuing it. Read AGENTS.md, the reset, current review/receipt and `bd show canopi-kqpp`; record this scope in bd. No subagents or direct reviewer exchange.

## Read and deliver

Inspect `scripts/raster-qualification/candidates.json`, existing bench manifests/locks where present, the GeoLibre reuse inventory, the qualification/migration receipts, and locally available source/build records. Existing observations are revision-specific; do not turn recorded registry facts into a claim that you checked today's registry. No downloads, dependency installation, engine/source builds, registry search campaign, new experiment, benchmark or private-fixture scan is needed. Missing local material is an explicit unknown, not a reason to expand the task.

Add **one concise section** named `Artifact decision brief after qualification reset` to `q-typescript-receipt.md`. Use these four parts:

1. **What exact artifact has evidence?** Record known package version, source revision correspondence, available artifact hashes and host/build identity for the saved Desktop retry. Distinguish independently reviewed cell/ledger facts from implementer claims, old evidence and unavailable provenance. Read saved evidence only; no patching identities or reruns.
2. **What decision remains?** The current declaration pins Whitebox source `9c0ff4f…`, while the recorded published build is 0.5.1 from another revision. State whether existing local evidence resolves that mismatch; if not, keep it unresolved. Do not silently select the published version.
3. **Smallest next step under each user choice.** Retain pin: name the locally evidenced source/build prerequisites and missing inputs for one isolated pinned-source build attempt. Deliberately adopt the published artifact: list which pin/contract changes need user approval and which observations remain reusable. Consider another engine/native route: mark as a separate user decision, not an automatic fallback. No speculative implementation plan or fresh dependency research. If exact commands cannot be verified locally, say so rather than inventing them.
4. **Decision-ready recommendation.** The main agent's starting recommendation is to retain the declared pin and resolve its build feasibility before spending more on harness repairs. Support or challenge that recommendation with inspected evidence; do not choose the artifact yourself. Name the one approval needed next, the most important remaining product risk and which single experiment would reduce it after approval. A documented unknown can be a valid result.

Target roughly one to two pages, using links rather than repeating history. Do not add a new schema, matrix framework, verifier, test suite or separate receipt. Keep private paths and raw user pixels out of committed text. No runtime/source/test file changes, no deletion of Python or harness code, no production N1, UI change, integration or release. Existing tests are preserved, not weakened; docs-only checks suffice.

## Acceptance and handoff

Acceptance is a coherent artifact/evidence table, an explicit unresolved decision, a recommendation traceable to inspected facts and one bounded next action—not a passing Q verdict or a working new build. Do not make resolving all unknowns a prerequisite to delivery. Update the index link and the reset's debrief outcome with facts only; preserve the review's remaining defect disposition. Report exact inspected revision/records and unavailable local evidence. Run `python3 scripts/check_docs.py` and `git diff --check`; code tests are skipped because no code changes are authorized.

Follow bead/export/commit/push rules, keep Q open, then stop for one review through the user. The next engine choice, build or experiment requires a new user-forwarded assignment. Do not auto-resume any retired repair prompt if the brief reveals a gap.
