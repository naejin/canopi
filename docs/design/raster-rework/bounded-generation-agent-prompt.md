# Bounded-generation initial assignment — superseded

Status: retired — initial B1–B5 kickoff at `ced5a1aa`; continued through `6a5130b3`, independently reviewed as partial rather than complete.
Tracking: `canopi-jv8a.4`, parent `canopi-jv8a`.
Current guidance: [sole current correction assignment](ordered-cog-agent-prompt.md), [independent review](bounded-generation-review.md), [design](ordered-cog-design.md), [LiDAR](../../agent/lidar.md).

Do not restart from `a5fc7d7b` or create a new implementation bead. Continue the delivered branch from `0696bd3d` using the ordered COG assignment. The original executable text is retained in Git at `ced5a1aa`; its source-merge design is superseded; preserve the compatibility and safety contracts named in the current design. An execution-window checkpoint does not require renewed architectural approval.
