# Remaining raster candidate qualification

Status: partial — named F1/F2b/R50 local repairs completed by reviewer execution after `82354620`; previous repair kickoff is retired.
Tracking: `canopi-j571.1` remains open; remaining qualification is `canopi-j571.3` under `canopi-j571`.
Current guidance: [current receipt](completion-receipt.md#current-correction-acceptance), [LiDAR guide](../../agent/lidar.md), [edition guide](../../agent/edition-development.md), [C0–C5](completion-design.md), [debrief](review-and-debrief.md#final-correction-debrief-to-deliver).

Do not restart the completed repair assignment. Read the current bead and receipt
before continuing qualification. Preserve the actual-command/native publication
proof, frontend caller tests and all accepted repairs. Use the integrated review tree identified in the current receipt once integration
verification completes; preserve user-owned primary files. Integration is separately
authorized under `canopi-t3ai`; release and branch deletion remain unauthorized.

Remaining evidence and exact known limits are recorded once in the receipt. Finish
feasible local qualification with existing tools; required private inputs, keys or
other OS hosts need their actual prerequisites. Scope stays fixed by C0–C5 and the
source-import amendment. Do not reopen Q, lift ceilings without evidence or add a
framework. Missing proof remains partial. Record real attempts, test boundaries and
observed results; no generic unavailable label or fabricated TDD RED. Continue
ordinary local execution without routine approval questions. Escalate material
product/scientific/persistence/ownership decisions only, with a concrete conflict.

Use the existing final debrief and receipt, update current rows in place, and return
one consolidated verified outcome with exact remaining limitations. The user owns
integration/release authority and remains courier if a separate agent is used.
