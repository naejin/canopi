# Raster rework — user-mediated implementation and review

Status: active — raster-specific application of the project-wide architecture/review workflow.
Tracking: `canopi-j571`; execution state and follow-ups belong in bd.
Current guidance: [architecture ownership](../../workflow/architecture-ownership.md), [AGENTS.md](../../../AGENTS.md), [delivery](../../workflow/delivery.md), [debrief](review-and-debrief.md), [handoff index](README.md).

## Ownership and courier boundary

The [project-wide agreement](../../workflow/architecture-ownership.md) owns responsibilities, architectural review triggers, delegated choices and escalation. Apply it here: the main agent owns architectural coherence and independent review; the implementation agent executes the fixed slice; the user remains courier and retains engine/UI/scope approval. This raster record adds evidence conventions, not a second authority or broader execution permission.

Forward the handoff path and commit, then return the delivery with commit/branch and receipt path. The repository artifacts must suffice without reconstructing chat history. A delivery is not acceptance; acceptance is not Q qualification, integration or release. Reviewer findings must cite an existing invariant, a reproducible observation, severity and the precise work it blocks. New requirements need user approval, not retroactive enforcement.

## One bounded loop

Use the project-wide handoff and consolidated-review rules. For this rework, the [index](README.md) names the sole current assignment, the standing review records independent dispositions, and bd owns execution status. Preserve accepted tooling repairs while measuring the next bounded capability. Neither a tooling pass nor a positive pilot qualifies all of Q or authorizes production N1. Existing prototype and engine-decision approval gates stay with the user.

## Delivery evidence and continuous improvement

For the Desktop completion slice, test the following bounded improvement: before delivery, trace each acceptance family from its original contract through the actual caller/adapter to an observable regression, not only a helper test. Put this compact trace in the existing receipt. The main agent must settle any missing terminal behavior before implementation and acknowledge design omissions in review. The implementer may complete every internal milestone without a courier checkpoint; the user forwards one consolidated delivery. Evaluate this intervention against escaped DB1–DB4 families at independent review, not test counts. It is not authorization for a broader audit or a new permanent reporting system.

The `f9b5c10c` review showed this intervention was incomplete: scripted launcher boundaries hid real process/lifecycle gaps. For the current lifecycle assignment, add what each detector mocks to that same trace. A mock may replace an external dependency, but not the cancellation, queuing, publication or deadline behavior being claimed. The main agent supplies concrete suspended-state acceptance examples; the implementer exercises the shared production adapter. This bounded refinement does not add another report or courier checkpoint.

Use one revision-labelled section in the existing receipt, not a new report per defect. Include: baseline/delivery commits; scope and deviations; implemented versus independently accepted versus genuinely measured; meaningful RED/GREEN and sensitivity evidence; exact gates/limitations; new real Q capabilities and product slices; Python replacements/deletions with remaining consumers; next blocked dependency and authority needed. State zero capability gain when the work is tooling-only. Test counts support verification, not productivity claims.

After each review, the main agent records the disposition in the standing review and adds only new, actionable process evidence to the debrief. Apply the project-wide design-omission / implementation-deviation / test-gap / reviewer-oversight categories, allowing overlap and uncertainty; name the escaped invariant, evidence and response owner. Record timing/cost/courier-cycle data only if observed. Do not infer model or language causes from one sequence of repairs.

For each proposed improvement, name the observed failure, a small change, the next slice on which it will be tested, and what evidence would show benefit. Adopt demonstrated lessons into a regression, focused guide or tooling; changes to skills/repo-wide workflow require their own authority. Retire old prompts, replace stale current guidance, and keep historical evidence revision-labelled. The final debrief compares accepted outcomes and measured effort across slices, not just message count.
