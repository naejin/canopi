# Bounded-generation product closure — retired

Status: retired — delivered through `0696bd3d`; replaced by the user-selected ordered COG Data Layer assignment.
Tracking: `canopi-jv8a.4`; follow-up `canopi-kko3` is included in the replacement scope.
Current guidance: [current prompt](ordered-cog-agent-prompt.md), [current design](ordered-cog-design.md), [debrief](review-and-debrief.md#ordered-cog-delivery-and-final-debrief), [delivery](../../workflow/delivery.md).

The C1/C2 collision-ownership and journal-clear repairs are accepted within their reviewed safety scope. The real Desktop smoke demonstrated import, replacement, slope and restart, but exposed stale History/map and dependent analysis after Undo. Product closure was not accepted; no integration or release occurred. Preserve the repairs and useful scientific/ownership tests.

Original executed instructions remain in Git at `94fd0a8d` (incorporated as `f64b2d4e`). The implementation branch retains `bounded-generation-receipt.md` and its historical evidence. Do not restart the old `6a5130b3` assignment or its source-merge scope freeze. The replacement design supersedes that architecture and authorizes completing the new workflow, including live Undo and slope refresh.
