# Raster foundation, Data and Analysis workbenches

Status: active — compatibility-first G1–G5 independently accepted at `a5fc7d7b`; bounded generations delivered through `6a5130b3` are partially reviewed, not accepted as complete or integrated. The [product-closure](raster-rework/bounded-generation-completion-agent-prompt.md) continues the [design](raster-rework/bounded-generation-design.md) on the same branch. Q stays frozen/unqualified. New workbench UI remains subsequent work.
Tracking: `canopi-j571`; current implementation `canopi-jv8a.4`, accepted predecessor `canopi-jv8a.1`, foundation `canopi-jv8a`, workbenches `canopi-j8mp`, historical qualification `canopi-kqpp`, deferred `canopi-5neg`.
Current guidance: [LiDAR](../agent/lidar.md), [MapLibre](../agent/maplibre.md), [edition development](../agent/edition-development.md), and [document lifecycle](../agent/document-lifecycle.md).

## Mandate and delivery boundary

**Current transition authority:** the bounded-generation design owns B1–B5, a coherent implementation of storage and all existing callers before capacity removal. It supersedes the old N1–N3/D execution split below for this batch. Keep review/Apply/preview and its `replace-overlap` option usable on new heads; later U introduces the single-action UI. Retain Float32 compatibility now, not an implied lossless precision migration. Retain standard prepared source COGs and persist resolved 1024×1024 spatial chunks as standard COGs; the revised design replaces the unimplemented raw/mask encoding at `24fd1a56`. Numeric Ready becomes independent of display-cache availability. Other product outcomes below remain future contracts, not additional scope silently attached to B1–B5.

Replace the dense-raster import foundation with bounded raster access inspired by GeoLibre, and organize Desktop geographic workflows around Data, Analysis, and Layers. A user imports numeric GeoTIFFs, chooses whether incoming data replaces overlap, runs a compatible analysis, and controls source/result display independently. Large files and large collections must not require a full-resolution mosaic in memory.

Include Web Location placement and shared basemap presentation in this initiative. Desktop alone receives Data import and Analysis in this delivery. Web continues to preserve dataset references without rendering or processing their local assets. Numeric rasters are in scope; local RGB/RGBA orthophotos are deferred. Web Location supports map placement and coordinates; address search is deferred. Preserve Desktop address search.

The first complete analysis is slope. Hydrology, canopy analysis, and globe rendering are later increments, not required to close the initial delivery. Prepare small reusable interfaces for the actual consumers in this plan; do not implement speculative engine abstractions or a general-purpose GIS framework.

No SQLite-to-DuckDB migration, general SQL console, plugin marketplace, visual processing model builder, split-map comparison, national data mirror, implicit upload, point-cloud import, service worker, or offline Web map archive is included. PDF map/raster export remains outside scope under [ADR 0024](../adr/0024-shared-canvas-pdf-export.md). Existing local Desktop raster use must work offline after installation/import; online basemaps retain provider-dependent availability.

This artifact retains the long-term product contract. The 2026-09-20 GeoLibre decision changes its execution order: preserve existing UI and features through native integration first, then complete the bounded foundation and add new features. Native integration is independently accepted at `a5fc7d7b`; the bounded-generation design owns the next batch and conditional limits. Future sections below do not expand that assignment. User review accepts consequential layout changes; production tests and packaged-platform evidence establish implementation claims, not completion of a temporary qualification framework.

### Start and continuation contract

On the current implementation instruction, read the [bounded-generation correction](raster-rework/bounded-generation-completion-agent-prompt.md), inspect Git status and `canopi-jv8a.4`, and continue from `6a5130b3` preserving accepted `a5fc7d7b`. Complete C1/C2 and evidence correction, then the real Desktop smoke under the scope freeze, before one consolidated courier delivery. Execution-window checkpoints need no new architecture assignment. Do not restart G1–G5 or Q or treat `bd ready` as acceptance evidence.

Work serially, one claimed bead and scoped branch at a time. Before a production slice, its bead must link its contract below, record satisfied prerequisites in `design`, and carry observable checks in `acceptance`. The existing coarse beads need this reconciliation at implementation kickoff; `canopi-j8mp.1` covers only the independently requested HTML reference authoring. Create the remaining bounded children specified in the execution table at that kickoff, reuse existing work, and leave blocked successors open. The slice names below are specification references, not a second status tracker; bd remains authoritative.

- **Engine decision:** the user selected useful GeoLibre components behind Canopi-owned modules. The first native dependency and retained GDAL roles are fixed in the integration design. Fetch/build that source directly; do not repeat the old npm qualification. A different revision, engine or scientific contract still requires a decision through the user.
- **Prototype decision:** P is satisfied for the HTML reference revision `0e696722`, explicitly approved by the user on 2026-09-18. Use the receipt below; do not request the same approval again. Material UI changes beyond that revision require renewed approval. Passing tests, silence, or approval of the written plan is not acceptance of a different prototype.
- **Scoped continuation:** complete all internal milestones within the current assignment without intermediate review requests. Missing fixtures/platforms block their evidence and release claims, not independent in-scope work. A failed product correctness/resource gate blocks dependent claims; no automatic engine substitution or next assignment follows delivery.
- **Integration:** pushing verified slice branches does not establish integration. Use the delivery workflow and retain all required commits in the combined checkout. If integration is not authorized by the implementation instruction, hand off the verified branches and remaining integration action. Never claim platform support from another platform's result.

## Evidence and existing work

Inspected baseline: `868ba7d65b3fc809a2cebbcd28158106ca8b5c29` (2026-09-18). The three commits since the earlier `a13f2d99d0864df81fb67fc61baacc24d7d476ff` inspection change only documentation and tracker metadata. Recheck before execution. The reported import error has not been reproduced through the application; metadata and code identify a relevant capacity failure, not a verified account of the user's original action.

| Surface | Inspected entry point and retained behavior |
| --- | --- |
| Native library | `desktop/src/services/lidar/{mod,catalogue,paths,import,grid,analysis,display,engine,probe}.rs`: dedicated SQLite catalog, managed originals, immutable generations, history/undo, generation-checked publication, slope refresh |
| Contracts and IPC | `common-types/src/lidar.rs`, `desktop/src/commands/lidar.rs`, `desktop/web/src/ipc/lidar.ts`: typed native boundary and versioned document presentation |
| Frontend library | `desktop/web/src/app/lidar/actions.ts`, `library-store.ts`: session-fenced presentation attachment, status, polling, import and analysis actions |
| Document authority | `desktop/web/src/app/design-edit/lidar.ts`: reference visibility, opacity, style, order, and removal through Design Edit |
| Current UI | `desktop/web/src/components/panels/lidar/LidarLayersSection.tsx`: combined management/presentation and import review, to be replaced by the three-surface workflow |
| Shared renderer | `desktop/web/src/app/canvas-map-surface/`: coordinator, contribution adapters, semantic layer stack, one camera/context, terminal failure handling |
| Basemaps | `desktop/web/src/maplibre/config.ts`: OSM street tiles, MapTiler satellite-v4 when `VITE_MAPTILER_KEY` exists; current missing-key normalization falls back to street |
| Location | `desktop/web/src/app/location/coordinate-workbench.ts`, `map-editing.ts`, `model.ts`; `components/canvas/LocationTab.tsx`: placement transaction is reusable, current view composes native address search |
| Edition shell | Frontend-relative `components/workspace/DesktopWorkspace.tsx`, `web/WebWorkspace.tsx`, `components/workspace/WorkspaceComposition.tsx`, `components/panels/PanelBar.tsx`, `app/shell-commands/`: capabilities and registered dock surfaces |

At accepted baseline `a5fc7d7b`, import still checks 512 MiB per file, 1 GiB and 16 files per batch, and 25 million cells per dense working grid. Source extraction and slope postprocessing are now bounded, but composition still allocates a full union grid/mask and import review builds union coverage and preview imagery. The B1–B5 caller migration addresses those remaining allocations before limits move.

Existing operating contracts to preserve: SceneStore owns local geometry; Design Edit owns non-canvas document edits; the library owns source bytes and scientific results. Preserve native executor admission, controller dependency rules, independent source/result visibility, coordinated document history, scoped asset access, and exact shared-map teardown. See [ADR 0025](../adr/0025-always-anchored-spatial-workspace.md).

### External reuse inventory

The table below is historical candidate research, not the current engine-selection queue. The selected native source, exact dependency pin, API and license rationale are in the [GeoLibre integration design](raster-rework/geolibre-integration-design.md#inspected-baseline-and-selected-reuse). Record the actual native build/lock in its production receipt; historical WASM measurements do not transfer to it.

| Candidate | Source identity | Decision and adaptation |
| --- | --- | --- |
| GeoLibre | `c5a72b027002a673ce48115ee19358205acd9791` | Adapt architectural patterns and selected pure definitions. Do not import its React/Zustand shell. Inspect `packages/map/src/cesium-cog-imagery.ts`, processing registry, and core dataset descriptors. Retain relevant upstream assertions when adapting behavior. |
| whitebox-wasm | `9c0ff4fdf3513f27b89c78e294610c3b418b3a4f` | Historical WASM candidate. The new native integration uses its `wbgeotiff` source as selected by GeoLibre's lockfile, not the mismatched measured npm artifact. |
| cog-tiler-wasm | `a71c321d357b0fde063238ab38bcf7ddb914eacd` | Evaluate reuse for overview selection, reprojection and display tiles. Verify local byte-range I/O, worker operation, numeric masks, and shared MapLibre context behavior. The inspected workspace says 0.3.1 while GeoLibre declares a newer package range: resolve artifact/source correspondence explicitly. |
| geolibre-rust / geolibre-wasm | Historical inspection `f568433ba6392c57509b418688cc8e4c2479e088`; current selection `aac2b743978666f3c3119b5c93de1b30963b1493` | Selective native-core adoption is now approved. Do not bundle the browser facade or whole tool registry merely for the decoder. Browser in-memory execution is not a large-raster capacity route. |
| Existing GDAL adapter | `desktop/src/services/lidar/engine.rs` | Retain as scientific reference and qualified native preparation/processing path while new components are evaluated. Native block processing is allowed; a complete replacement of GDAL is not promised. |

Sources: [GeoLibre architecture](https://geolibre.app/architecture/), [COG tiler](https://github.com/opengeos/cog-tiler-wasm), [Whitebox](https://github.com/opengeos/whitebox-wasm), [GeoLibre Rust](https://github.com/opengeos/geolibre-rust). Whitebox's wasm32 ceiling and whole-raster operations require qualification. Existing source availability does not establish numerical correctness, packaged compatibility, or bounded execution.

Canopi's raster-module maintainer owns upstream pins, license notices, adapter tests, and upgrade qualification. Prefer upstream libraries over copied implementations; copied definitions require source/license attribution and a retained behavior test. Do not copy generated forms that expose arbitrary engine flags or add React/Tailwind/Zustand dependencies.

## Product and domain decisions

### Dataset, analysis, and presentation

A dataset has stable identity, editable name, immutable measurement interpretation, and a current immutable generation. Source interpretation records numeric type, band, scale/offset, units, horizontal CRS, vertical reference where meaningful, resolution and validity rules. Dataset interpretation fixes measurement meaning and the aligned lattice; each generation records its changing extent and coverage. A filename is a hint, never authority for measurement type.

An analysis definition records compatible input dataset identities, recipe/version, parameters, resolution, engine identity, and output meaning. The initial operation accepts one ground-elevation input and produces slope in degrees or percent. Results retain exact input generations, numeric outputs, validity/quality masks, and provenance. Later multi-input operations must declare compatibility and dependencies explicitly.

A display layer is a Design-owned presentation reference to a dataset or result. Hiding, reordering, or restyling does not mutate numeric data or start computation. Multiple Designs can reference one library dataset. Renaming or updating that library dataset does not dirty every Design. Adding/removing its presentation does dirty the current Design through Design Edit. Imported raster geography never changes when the Design anchor moves.

Preserve the existing serialized `lidar` presentation section and stable IDs initially, even though UI terminology becomes Data and Analysis. Do not rename the format just for UI vocabulary. Continue format-6 admission and Web round-trip behavior. Any schema extension must be additive and retain unknown per-entry fields according to existing contracts; a breaking format change requires a separate decision.

### Right-side surfaces

Data and Analysis become registered side surfaces in the existing resizable dock, adjacent to Layers in the Design group. Opening them keeps the canvas mounted. Use the existing Preact components, tokens, focus-return behavior, keyboard access, and all 11 locales. Prototype the consequential layout before production changes, following the [dock contract](../../.interface-design/patterns/dock-panels.md).

Data lists reusable library datasets and their interpretation/status. Its primary action imports files into a new dataset or a chosen compatible existing dataset. The same surface provides rename, add to current Design, import history/undo, and library deletion. New imports attach a visible reference to the originating Design only if that session still exists. Existing-dataset updates preserve current presentation preferences.

Analysis starts with operation selection, followed by compatible inputs, result name, units/parameters, and Run. Display why an input is ineligible; do not silently convert MNH into ground elevation. Keep unsupported future operations out of the working catalog. Closing/switching panels leaves jobs running. Explicit Cancel cancels the job; Escape closes a non-mutating form but must not silently cancel an unrelated running job.

Layers contains presentation controls for datasets and results, alongside the existing scene stack and site references. Geographic sources/results share an explicitly ordered geographic band beneath grid and Design objects. They are not active scene drawing layers or draggable Design objects. Provide independent visibility, opacity, color ramp/legend, and Zoom to extent. Source grouping, if used, must not constrain result visibility or actual saved order.

Pixel inspection is read-only: select the geographic layer to inspect, then show the physical numeric value and units or explicit NoData/unknown. Read native numeric data, not a colorized display pixel. It must not steal normal canvas editing gestures; the prototype must validate an explicit inspection mode and keyboard dismissal. Display sampling cannot change analysis resolution or source values.

Prototype one concrete interaction proposal: Data / Analysis / Layers are consecutive dock commands; Data rows expose name, measurement, coverage and job state, with secondary rename/history/delete in `ActionMenu`. Import opens a dock form containing the ordered file list, new/existing destination, interpretation and replacement checkbox. Analysis shows only Slope, lists all datasets with disabled ineligible rows and reasons, and exposes result name and Degrees/Percent. Run attaches its definition to the originating active Design and then shows job status; a failed initial job remains an honest failed result that can be retried. Layers uses a flat geographic list in actual display order, source/result badges, independent eyes, and keyboard-accessible move-up/down controls; settings appear below the list. No source nesting constrains order. Keep default ramp colors from the existing display owner, add a grayscale alternative, and show numeric min/max plus units. Degree slope retains its fixed 0–90° stops; percent slope distributes those colors across its exact observed percent min/max, without interpreting degree thresholds as percentages. Source/grayscale domains use exact generation min/max. Constant-valued data gets a constant legend, never division by zero. Saved unknown style strings round-trip unchanged and render with the default plus an unsupported-style indication.

The explicit Inspect action toggles a canvas mode for the chosen source/result. Pointer click samples; pan/zoom remains available, drawing/selection is suspended until Escape or toggling Inspect off. A keyboard action samples the canvas center so mouse use is not required. Show the selected layer, location and result in a read-only status surface; exit on Design replacement, layer removal, Location navigation or runtime teardown. Hiding that layer ends its inspection mode. Closing the dock alone does not cancel import/analysis; reopening restores the active job. Failed jobs expose Retry as a fresh job against the current head, with the preserved request shown for confirmation. Escape dismisses an unsubmitted form or inspection mode; only the job's explicit Cancel requests cancellation.

Hide affects visibility; Remove from Design removes only the presentation reference and is undoable in document history. Delete from library confirms the dependent analysis count and current Design reference count, with the existing warning that other saved Designs may reference the entity and will show it as unavailable. Do not scan saved Designs or invent a complete cross-Design reference count. Deletion removes current-session references through Design Edit only if that same session is still active; other saved Designs remain untouched. Retain current library history/undo semantics; library undo is separate from document undo. Missing references remain preserved with an unavailable state rather than being silently discarded.

### HTML UI reference contract

The [reference hub](../../desktop/web/ui-gallery/raster-reference-prototype/index.html) is the concrete proposal for P, authored before Q at the user's explicit request. **Approved by the user on 2026-09-18 at commit `0e696722`**, covering the hub and all seven HTML references and their linked states in the table below. Approval is recorded in `canopi-j8mp.1` and its parent `canopi-j8mp`; P is satisfied for this revision. Do not rebuild another initial prototype or ask for the same approval again. Material deviations require renewed user review and updates to these references and the affected product contract. Approval accepts the UI proposal only, not scientific correctness, engine qualification, production implementation, integration or release.

Serve with `cd desktop/web && npm run dev:ui`, then open `http://127.0.0.1:1422/raster-reference-prototype/index.html`. These are HTML entrypoints, not `?surface=` stories. They require the shared `reference.js` and `reference.css`; the stylesheet imports the real application tokens. Do not copy an HTML file alone or use `file://`. The hub links every state; append `?state=<state>&theme=dark&edition=web&width=narrow` as needed. Defaults are light/Desktop/full width. The reviewer toolbar changes those options without changing production settings.

| Production slice | Exact HTML reference | States and interaction evidence to consult |
| --- | --- | --- |
| U — Data | [data.html](../../desktop/web/ui-gallery/raster-reference-prototype/data.html) | `ready`, `empty`, `loading`, `error`, `busy`, `history`, `delete`, `rename`, `missing`, `long`. Library ownership, Add to Design, secondary management, deletion impact and separate library undo. |
| U — import form and job | [import.html](../../desktop/web/ui-gallery/raster-reference-prototype/import.html) | `ready`, `existing`, `progress`, `complete`, `no-change`, `cancelled`, `error`, `busy`, `incompatible`, `disk-full`, `interrupted`, `session-changed`. Ordered files, required interpretation, unchecked replacement, single Import action, preserved heads and late completion. |
| U — Analysis | [analysis.html](../../desktop/web/ui-gallery/raster-reference-prototype/analysis.html) | `ready`, `empty`, `running`, `complete`, `refreshing`, `failed`, `cancelled`, `engine-missing`. Disabled incompatible inputs, units, named result, retry and previous-result visibility. |
| D / U — presentation | [layers.html](../../desktop/web/ui-gallery/raster-reference-prototype/layers.html) | `ready`, `empty`, `missing`, `tile-error`, `refreshing`, `unknown-style`, `removed`, `long`. Flat order, independent eyes, keyboard move controls, opacity/ramp/legend and undoable removal. D uses this for rendering relationships; U owns the production dock controls. |
| I — numeric inspection | [inspection.html](../../desktop/web/ui-gallery/raster-reference-prototype/inspection.html) | `ready`, `value`, `no-data`, `loading`, `unavailable`, `inactive`. Explicit mode, sample center keyboard action, physical units, distinct NoData and Escape dismissal. |
| W — placement | [location.html](../../desktop/web/ui-gallery/raster-reference-prototype/location.html) | `provisional`, `selected`, `confirmed`, `map-error`, `invalid`, `cancelled`, `undone`, `session-changed`. Review with `edition=web` and `edition=desktop`: coordinates versus retained Desktop search, preview/confirm/cancel/undo and map-failure entry. |
| B — provider settings | [basemaps.html](../../desktop/web/ui-gallery/raster-reference-prototype/basemaps.html) | `keyless`, `official`, `loading`, `invalid-key`, `quota`, `offline`, `maptiler-missing`, `attribution-error`, `save-error`. Review both editions: provider choice, shared opacity, optional official key, explicit errors and attribution. |

Implementation use: read the relevant row and product/scientific contracts, open its normal and failure states, then rebuild the accepted layout using production Preact primitives, CSS Modules, i18n and existing workbench owners. Reuse production `ActionMenu`, fields, buttons and dock focus conventions; the mockup's native HTML equivalents are not a new component system. Do not import this prototype's JS/CSS or promote it into `src`. Preserve the existing production shell/navigation; the compressed reference rail and illustrated canvas provide context, not a shell redesign. The previous `lidar-prototype/` study is historical and is not the reference for the new import or three-surface workflow.

The dark/light reviewer strip, state selector, Switch Design, Reset and Advance simulated job are review-only controls. All files, map geography, values, generations, jobs and credentials are synthetic. No persistence, network provider, native picker, numeric computation, camera implementation or document history is proven here. Scenario URLs specify independent visual fixtures, not an exhaustive executable state machine. The Layer/Inspect examples illustrate ground elevation; production must derive the chosen layer's actual units/range. Web retains unavailable local references, not working local raster controls. Exact numeric algorithms, lifecycle fencing, provider storage and all edge-case behavior remain governed by the contracts below, not mock values or simplified event handlers.

P review walks Data → Import → job → Analysis → Layers → Inspect, and separately Web/ Desktop Location → Basemap. Exercise cancel/retry, panel switching during a job, independent eyes/order, Remove versus Delete, keyboard sampling/Escape, invalid coordinates, placement undo and provider failures. Review desktop and 390px width in both themes, plus `long` labels. English long-label fixtures do not prove all 11 translations: U/W/B must add localized production-gallery fixtures and test focus/overflow with actual strings. Acceptance records any agreed deviations; unresolved material differences stop the affected production slice for review.

### Simple, bounded import

The user chooses files, destination/measurement interpretation, and one unchecked-by-default option: **Replace existing data where these files overlap**. Import begins the job; no Before/After preview, per-region decisions, or second Apply stage remains.

Uncovered valid pixels are always admitted. With replacement off, existing valid pixels win; with replacement on, incoming valid pixels win. Incoming NoData/non-finite/masked pixels never erase existing data. Sort by case-sensitive filename Unicode scalar order, with original picker index as the tie breaker, and display that order before Import. The native request retains these ordinals rather than sorting again by path or locale. First valid input wins with replacement off; last valid input wins with replacement on. The summary states that ordering; no detailed overlap editor is introduced. Deduplicate original bytes by hash, but do not skip a reimport merely because the bytes already exist: after another source replaced its pixels, reimport with replacement on may restore them. Publish no new generation when resolved physical values and validity are unchanged; discarded no-op input must not change future undo semantics.

Probe all selected inputs before publication. One incompatible/corrupt file fails the batch with a named reason; no silent partial import. An all-NoData or entirely redundant batch reports no change. Import history records the sources, ordering, replacement choice, and resulting generation.

Process one bounded set of source blocks at a time, incrementally persisting prepared numeric tiles/COGs, validity, statistics, and provenance. Original hashing/copying is streamed too. Resolve accepted coverage through immutable source/member references and block-level precedence; never rewrite a full dense mosaic when a source changes. Publish the batch through one short catalog transaction after durable staging is complete. Remove fixed file-count/file-byte/dense-union ceilings as capacity policy once their replacement resource gates pass; retain arithmetic, format, disk-space, and bounded-allocation checks.

Keep original files byte-identical in managed library storage. Preparation creates derivatives; no user original or sidecar may be rewritten. Reuse existing mask and numeric semantics, including valid zero/negative values. Initial support remains single-band continuous numeric GeoTIFF, north-up and without rotation/reflection. For the future bounded foundation reject explicit alpha/dataset masks and nonidentity scale/offset before head publication; accept declared NoData and its derived mask, NaN and non-finite invalidity. Do not silently drop a mask or treat zero as invalid. Preserve legacy admitted data through the legacy reader. Record the supported numeric-type/CRS matrix in production tests/receipts; the future precision-preserving contract must not silently narrow through Float32 conversion. G1–G5 explicitly preserve today's Float32 extraction and do not claim that future contract. An unsupported type gets a named admission error. Do not promise automatic cross-CRS/grid/vertical-datum mosaicking: independent datasets may have different supported CRSs, but adding sources requires compatible interpretation and aligned grids. Broader mask, reprojection or resampling admission is a separate measured extension.

This is one user batch processed internally in blocks, not a request that users manually split files. Both one large raster and many small rasters are required acceptance cases.

## Architecture, ownership, and recovery

Keep four caller roles: library management, numeric access, processing, and display. Library operations return job/receipt identities; UI never sequences catalog transactions, engine processes, or raster buffers. Numeric reads take an immutable generation plus bounded window/resolution and return physical values with exact validity. Processing owns recipes and persistent results. Display owns derived tile requests, caches, and legends. Configure engines and file access once at composition, not through every UI call.

Use local managed GeoTIFF/COG assets with SQLite metadata. Do not store pixels as individual SQL rows. Numeric windows and display tiles may share source access while retaining separate caches and semantics. Use full-resolution data for analysis, appropriate overviews for display, and recorded reduction rules for previews/statistics. Loading a dataset must not precompute an entire world tile pyramid before it becomes usable.

The library owner survives panel changes and Design navigation. It owns job cancellation, bounded queues, file handles, engine workers/processes, and shutdown settlement. The existing native executor remains the only admission path for native heavy work. Display decoding must run off the UI thread when using WASM. Introduce a worker only with explicit creation, request-generation fencing, cancellation, partial-init cleanup, and teardown ownership.

One import or history undo may mutate a destination dataset at a time; reject a second mutation with a typed busy status, without creating a second active job. Reads use immutable heads. An import records its starting head and rechecks inside the publication transaction; if it has changed, fail with retry guidance rather than silently applying replacement against different data. Jobs must release catalog locks during compute/I/O. Deletion cancels and settles jobs for that entity before removing owned files; completion must also recheck entity existence. Reads already admitted hold their generation assets until settlement.

On source changes, preserve existing automatic dependency-aware slope refresh. Keep the last complete result visible, marked refreshing/stale, until replacement succeeds. A failed/cancelled refresh keeps that prior result and its provenance. Results from stale input/definition generations never publish; coalesce refreshes to the latest generation. Hidden inputs/results remain eligible for refresh. Display pan/zoom/style changes do not enqueue scientific work.

Disk exhaustion, malformed input, missing engine, cancellation, or worker failure must leave the prior dataset head usable. Publication acknowledgment follows durable data and catalog state. On restart, reconcile staged/unpublished jobs idempotently; preserve committed heads and mark interrupted work explicitly. Remove abandoned temporary outputs, never automatically delete authoritative originals/history. Reproducible display caches may be evicted under a bounded LRU policy. Preserve scoped asset serving; raw paths and credentials do not enter `.canopi` or diagnostics.

The Shared Spatial Workspace continues to own MapLibre, camera, shared Pixi context, layer ordering, and Canvas2D fallback. Raster adapters only contribute stable descriptors/data. A failed raster contribution reports dataset-local status and leaves editing usable when cleanup succeeds; failed resource cleanup follows the existing terminal map failure path. Cancel stale tile requests on generation replacement or teardown. Cache keys include immutable content/interpretation/generation and display style, not just dataset names.

### Implementation contracts

Keep `LidarLibrary` as the public native owner. Add private modules below `desktop/src/services/lidar/` for bounded numeric access and generation resolution; do not create a competing library service or rename the subsystem. The following operations and payloads are **future planned interfaces**, not existing symbols or current G1–G5 scope. Production tests must prove their physical operations when implemented. Third-party objects, paths, engine flags and full rasters never become component parameters.

| Caller role | Request and observable response | Owner and constraints |
| --- | --- | --- |
| Import | `startImport({ destination: existing ID or new name/interpretation, sources: ordered native selections, replaceOverlap })` returns job ID and reserved dataset ID. Job polling returns phase, completed/total work when measurable, and terminal `published(generation ID)`, `noChange`, `cancelled`, or named error. | `LidarLibrary`, commands and generated contracts; new dataset stays provisional until successful publication. Failed/no-op new imports create no visible empty dataset and attach no Design entry. A reserved ID is not an accepted head. The frontend submits once; no stage/apply orchestration. |
| Numeric access | Resolve an entity to an immutable generation descriptor, then `readWindow(generation, { x, y, width, height }, cancellation)` returns same-sized row-major physical values, exact validity and grid/units. | Native internal seam, usable for source or result. Integer pixel coordinates refer to that generation's north-up lattice; window is half-open, out-of-extent pixels invalid, dimensions checked before allocation. Scientific reads always use native resolution. Buffers are at most 1024×1024 cells plus a one-cell halo on each side. Use f64 in the resolver; reject unsupported precision at production admission rather than silently rounding large integers. No full raster crosses IPC. |
| Processing | Create a named slope definition for one dataset ID and units; return definition/job IDs. Resolve input generation once, stream core windows with halo, publish a new immutable result and provenance. | `analysis.rs` and library scheduler. Keep recipe version, definition revision, engine artifact identity and input generation in the receipt. Renaming changes no recipe; parameter edits create a new definition revision and invalidate stale publication. |
| Display | Request a bounded XYZ tile for entity/generation, canonical style and cancellation; return encoded image bytes or an explicit empty/unavailable outcome. Legend query returns the same style domain and units. | Display owner resolves accepted coverage before rendering/reduction. Native access is authorized by immutable identity; any WASM worker byte reads use opaque generation-scoped handles and bounded offset/length requests, never arbitrary paths. If a browser bridge is later selected, its production tests must prove local access rather than an HTTP-only demo. Keep the existing map contribution owner and semantic ordering. |
| Inspection | `sample(entity, expected generation, WGS84 point)` returns generation ID plus `value(value, units)`, `noData`, or `unavailable`. | One native pixel lookup through the numeric seam; inverse CRS transform followed by the containing pixel, no display interpolation. Cancel/supersede on pointer, selected layer, generation or Design change. Never display a stale response as the current sample. |

Errors crossing IPC carry a stable category (`busy`, `unsupported`, `invalidInput`, `noSpace`, `engineUnavailable`, `stale`, `cancelled`, `internal`) and safe user-facing detail; preserve source basenames where needed, never absolute paths or credentials. Translate category/UI framing in all locales. Routine implementation details such as private helper names remain the implementer's choice.

**Scientific recipe.** Preserve the existing GDAL slope behavior: Horn 3×3 derivative, horizontal and vertical metres, no edge interpolation, degrees by default or percent. Full 3×3 validity is required even when the center is valid. Obtain halo pixels through the resolved generation, including across member boundaries, then write only the core. Require projected metre grids and declared metre elevation for slope; display-only data need not be slope-eligible. A geographic-degree grid or unknown vertical units receives an eligibility reason rather than an assumed scale of one. Unknown vertical datum is recorded as unknown; no datum transformation is performed. The analytical oracle is `atan(sqrt(a*a+b*b))*180/pi` or `100*sqrt(a*a+b*b)` for `z=a*x+b*y+c` in metres. Compare degrees within 0.001° and percent within 0.01 percentage points on the test planes; validity must match exactly. These are acceptance tolerances, not claimed survey accuracy. [GDAL slope reference](https://gdal.org/en/stable/programs/gdaldem.html).

**Generation representation.** Preserve the existing ordered member replay semantics, including legacy `add`, `replace` and `replace-overlap` roles. The later single-action import emits only `add` or `replace`; B1–B5 retains `replace-overlap` while the existing review UI remains active. A generation owns immutable member occurrences keyed by import job and ordinal; the same content may occur more than once. Generation construction queries intersecting members through the existing footprint index and replays them in publication order for a bounded window. Persist resolved occupied COG chunks before publishing, sharing unchanged references; published reads need not replay all history. Retain prepared source COGs with source-specific validity; never create an authoritative dense union mosaic. The bounded-generation design owns the exact file profile, composed validity and separate quality contract. Use a disk-backed sparse occupied-chunk index and paged member queries; do not enumerate empty bounding-box cells or hold every chunk/member in RAM. Exact final statistics are streamed over resolved occupied chunks, not accumulated from overwritten source extrema. History undo removes that job's occurrences and replays the remaining sequence into a new head; it does not remove every occurrence of the same hash. Retain the current explicit inability to undo legacy imports lacking durable member history.

**Admission and metadata.** The first successful import fixes dataset interpretation and lattice. Extent/coverage belong to each generation and may grow; they are not immutable dataset interpretation. Store source metadata separately from the normalized measurement contract. Preserve current grid tolerances from `RasterGrid::compatible`; use qualified CRS equivalence rather than filenames. Keep same-CRS admission conservative if equivalence cannot be established. The form defaults interpretation to unselected, requiring explicit Ground elevation / Surface elevation / Above-ground height / Other continuous. Elevation and height choices use metres in this delivery; Other continuous requires a unit label or explicit unknown. Sidecars may provide evidence but never silently override embedded CRS. Production CRS tests record whether the supplied incomplete EPSG:2154 WKT is usable; failure is an admission/compatibility gate, not permission to rewrite the original.

**Resource policy.** Initially admit one heavy raster job library-wide and reject additional user submissions as busy; retain only the newest pending automatic refresh per analysis definition. Share this scheduler across import, undo, analysis and native preparation. Allow at most two active display/numeric read requests and 32 pending display requests, replacing obsolete viewport work first. A job must not hold an executor permit while waiting for child work queued behind that same permit. Reserve 128 MiB total for decoded numeric/display caches and 512 MiB on disk for reproducible display cache; evict only unleased cache entries. The 1 GiB measured gate includes these caches and every raster worker/child, not just one operation. Source lists and catalog rows are metadata, but must be streamed/paged where they can grow. Check free space against the engine's measured conservative staging estimate plus a 256 MiB reserve before heavy work, then check incrementally; any write failure rolls back publication. Do not impose a new fixed source count or pixel ceiling. The library owns scheduler and worker teardown, including HMR disposal of frontend adapters.

**Durable publication.** Stage on the destination volume, validate outputs, flush data and manifests, atomically rename to an immutable location, then commit catalog members/head/job outcome in one transaction with the starting-head and entity-existence checks. Use SQLite durable commit settings and platform-appropriate directory durability; record their tested guarantees. A crash before commit leaves reclaimable unpublished derivatives; a crash after commit leaves a complete readable head even if the client missed acknowledgment. Restart identifies publication by job ID and never republishes it. Cancellation before the commit boundary publishes nothing; a commit already completed reports success, not cancellation. Original objects and accepted history are excluded from automatic scratch/cache cleanup. A numeric head becomes Ready independently of display tile availability; a tile failure marks display unavailable without rolling back sound numeric data. Update the old Ready-after-pyramid operating rule in the implementing slice.

### Migration and compatibility

Preserve library IDs, original hashes, history, analysis definitions, and Design references. Keep physical legacy directory/catalog names initially to reduce migration risk. Add versioned prepared-cache artifacts rather than destructively converting originals. Existing ready generations remain readable during rollout; convert prepared assets lazily through a versioned cache adapter. One immutable generation may reference legacy and new prepared members, but a result must never switch input generation during processing.

If catalog changes are necessary, use the existing migration owner with backup and transactional schema changes; old binaries must reject unsupported schemas rather than reinterpret them. Abort interrupted legacy awaiting-review jobs without applying them, retain their original source bytes, and offer reimport through the existing review flow during B1–B5 (the simple flow comes with U). No old preview job is silently approved. A failed qualification retains the existing application path; do not ship an automatic fallback that reintroduces unbounded allocations for large inputs.

The inspected catalog is version 6. Allocate the next available version at implementation after checking latest main; use 7 if still free. Add the generation storage discriminators fixed in the bounded-generation design and versioned asset metadata without changing IDs. Back up with a SQLite-consistent snapshot that includes WAL state, not a live main-file copy. The legacy reader performs bounded seeks/windows on existing values/masks/TIFFs and never calls the dense full-file helpers. Existing generations remain immutable; changing their prepared-cache format alone must not create a new scientific generation or dirty a Design. New published heads use the new storage discriminator. For histories whose legacy assets cannot support exact replay, fail explicitly and preserve the old head. Schema migration, interrupted migration, unknown future schema, legacy history, and mixed old/new member reads require fixtures. Do not remove old readers while any preserved generation depends on them.

Keep existing generation/head/member tables as the identity authority. Make legacy dense-path columns nullable for new-format generations, with checks requiring them for legacy rows; never write fake paths to satisfy the old `NOT NULL` shape. Add storage version/manifest metadata for source and result generations and sparse block-index rows containing bounds, asset references and aggregate statistics, never individual pixel SQL rows. Preserve all foreign keys/indexes when rebuilding a table and run foreign-key integrity checks before committing. A versioned manifest identifies the lattice, units, validity encoding, ordered-member index, prepared artifact versions and engine identity; large member/block lists live in the paged catalog, not one giant JSON buffer. The bounded-generation design fixes the controlled standard COG profile and changes prepared derivatives from temporary files into managed immutable source assets, with separate non-deleting reader ownership. Definition names/revisions and structured job outcomes are additive migrations owned by their slices. Keep the `.canopi` presentation schema and format version unchanged.

## Web Location and basemaps

Adapt the shared placement transaction to a browser-safe Location surface. Latitude/longitude entry validates the finite Mercator-bounded domain from ADR 0025; map click or current-center placement produces the same candidate frame. Confirm commits one undoable edit, Cancel/Escape/unmount aborts it, and Design replacement fences late map events. Preserve altitude metadata and north bearing; no automatic elevation lookup or new altitude editor is included. Coordinate entry remains usable when the map cannot load. Geographic backgrounds may load in the explicitly opened Location editor, while provisional/hidden Canvas basemaps retain their no-remote-request rules.

Keep Desktop's address-search capability at its edition adapter. Browser production imports must not reach native geocoding or Tauri. Web draft/save/export/open must retain confirmed placement and dataset references. This phase requires an explicit replacement ADR for the relevant scope in ADRs 0013 and 0016; it does not override the static deployment or no-PWA contracts.

Share street/satellite visibility, opacity, attribution, and provider availability between Location and Canvas without introducing another simultaneous workspace camera. Switching basemaps must preserve the scene runtime, raster layers, selection, and camera. Keep settings ownership for existing basemap preferences. Provider keys stay device-local and out of Designs/exports/logs; public browser credentials require provider-supported restrictions and are not secrets merely because they are in frontend configuration.

**Decision S1 — Google satellite access (user-selected):** match GeoLibre's dual-path behavior in Desktop and Web. Selecting Google Satellite without a configured Google Maps API key uses `https://mt1.google.com/vt/lyrs=s&x={x}&y={y}&z={z}` as a MapLibre raster source. In the basemap controls, show the non-blocking message **“Enter a Google Maps API key to load the official Google tiles.”** Provide a labelled key input and a link to Google's key setup documentation. Do not require a key to select or display the keyless layer, and do not describe this endpoint as an authorized free third-party service. Authorization for free third-party use and service guarantees have not been established; this is a recorded limitation, not an unresolved product choice.

When the user supplies a key, request an official Map Tiles API satellite session and load `https://tile.googleapis.com/v1/2dtiles/{z}/{x}/{y}?session={session}&key={api-key}`. Keep the key in device-local settings, never in a Design, export, telemetry, or error text. Mask the input. Clearing the key explicitly returns to the keyless path. Retain existing MapTiler support as a separate provider option; existing saved provider choices must not silently change.

The basemap adapter owns official session acquisition, expiration/renewal, attribution, request cancellation, and generation fencing. Key changes and provider switches invalidate pending session work; late responses cannot replace the currently selected source. An invalid key or official service/quota failure shows an actionable provider error and allows editing/removing the key; do not silently downgrade configured official access to keyless access. Keyless failures also show unavailable imagery with the key-entry action. Neither failure blocks canvas editing, raster processing, or Location coordinate entry. Source replacement preserves camera, scene, data layers, and opacity. Validate current official session/attribution requirements during provider implementation; this decision does not assert billing-free official usage.

References: [GeoLibre basemap catalog](https://github.com/opengeos/maplibre-gl-basemap-control/blob/main/src/lib/core/catalog.ts), [endpoint selection](https://github.com/opengeos/maplibre-gl-basemap-control/blob/main/src/lib/core/BasemapControl.ts), [Google policies](https://developers.google.com/maps/documentation/tile/policies). Provider outages omit only that background with an honest unavailable state; never label street imagery as satellite. No bulk download, persistent offline imagery cache, or print use is authorized by interactive availability.

### Provider and Web implementation contract

S1 remains the fixed user choice. Its acknowledged keyless-service limitation is not permission for an executor to choose another product behavior, assert authorization, or claim service guarantees. Public release remains separately authorized. If actual access or attribution cannot be made to work, report the failing provider gate; do not remove the warning or substitute another source.

- Extend the generated `BasemapStyle` catalog with `google_satellite`; retain `street` = OSM and the serialized `satellite` = MapTiler. Default stays `street`. Show explicit provider labels, and preserve old saved values even when MapTiler's build-time key is absent; report unavailable instead of normalizing the saved choice to street. Google key presence chooses its official/keyless path, not a different style identity. Do not change world-template maps to Google implicitly; give that caller an explicit street presentation if required when removing its dependency on global style.
- Add nullable `google_maps_api_key` to shared device-local Settings with a null default; use the existing `SettingsPlatformAdapter`, settings projection and Desktop/Web storage owners, not a new persistence system. Mask input, trim on explicit Save, support explicit Clear, and display save failure. Do not log/Debug-print complete Settings. The key is a browser credential, not a secret vault guarantee. Keep it out of Design schemas, sample fixtures, recorded network traces, error text, exported diagnostics and screenshots used for evidence. Native `problem_report/bundle.rs` must retain its explicit safe settings allowlist. Provider adapters sanitize errors before any console/diagnostic sink; test URL, response-body and thrown-error paths with a recognizable fake key.
- Introduce a shared provider module under `maplibre/` returning `idle | loading | ready(descriptor, attribution) | unavailable(safe reason)` with `update(presentation, viewport)` and `dispose()` operations. Configure its HTTP capability once at edition composition; browser uses bounded fetch, Desktop uses the executor-backed native network adapter when required by WebView access. Each active map surface owns one subscription and its cancellation; shared settings own no map or camera. An invisible/provisional Canvas requests neither sessions nor tiles. Location explicitly admits its own basemap while mounted; existing primary-route ownership keeps it from creating a second simultaneous Canvas workspace.
- Google official requests use `createSession` with `mapType: satellite`, the app's supported locale mapped to an IETF tag, and a validated locale region or `US` fallback. Store the returned session only in memory; honor its `expiry` and tile dimensions. Renew once within 60 seconds of expiry while visible, or on next admission if hidden; an authentication failure requires user correction. One renewal is in flight per provider generation. HTTP requests time out after 15 seconds; transient network/429/5xx responses get at most two retries with jittered 1s/2s backoff, respecting a longer `Retry-After` by reporting retry availability rather than holding work indefinitely. No retry for invalid credentials. Cancellation clears retries/timers and fences late completion.
- Acquire viewport copyright/availability metadata before displaying official tiles, then refresh it on settled viewport changes. Keep Google Maps and returned data attribution visible and distinct from Canopi overlay attribution; a generic static copyright string is insufficient. Fence viewport responses as well as sessions; omit imagery whose required attribution is unavailable. Use returned availability to cap source requests while preserving Canopi's camera zoom/overzoom. Honor provider HTTP cache rules and use no app-managed persistent imagery cache. The keyless path uses visible Google Maps attribution, without fabricating official viewport metadata or licensing assurances.
- Bind changing source descriptors to the current map through the existing contribution reconciliation. Do not call `setStyle()` or key map lifetime on provider/key/session changes. Refactor Location's current `key: preferredBasemapStyle` rebuild in `app/location/map-editing.ts` to incremental contribution updates, preserving candidate placement, pin and camera. The provider module owns sessions and attribution; `workspace-map-controls.ts` keeps map mutation/teardown ownership.
- The shared Location view receives an optional search capability. Desktop composes its existing native search; Web imports the coordinate workbench directly, never the Desktop `app/location` search barrel. Coordinate parsing rejects empty/non-finite/out-of-Mercator values and commits the same complete spatial frame as map placement. On blur/Enter valid coordinates preview only; Confirm alone commits. Cancel/Escape/unmount abort; map pan never confirms. Preserve altitude and north bearing. Establish the replacement ADR for the specific Web restrictions before updating architecture guards; retain guards banning native geocoding/Tauri imports, PWA behavior and local-raster processing in Web.

Verified reference contracts on 2026-09-18: [session requests and expiry](https://developers.google.com/maps/documentation/tile/session_tokens), [viewport metadata](https://developers.google.com/maps/documentation/tile/2d-tiles-overview), and [attribution/cache policies](https://developers.google.com/maps/documentation/tile/policies). Verify these again when implementing the provider; if changed requirements conflict with S1, stop that slice with concrete evidence rather than inventing compatibility. Live official qualification requires a user-supplied restricted key in an isolated profile; absence is a named verification blocker, never a reason to purchase access or put a key in fixtures.

## Representative evidence and capacity acceptance

Originals are read-only and stay outside Git. Previously inspected with `gdalinfo -json`; SHA-256 streamed without mutation. Run probes with GDAL auxiliary-metadata writes disabled, and direct all preparation/statistics outputs into managed temporary derivatives. Qualification verifies source and sidecar hashes before/after. Only small synthetic fixtures and reproducible manifests belong in the repository.

| Fixture | Verified facts and role |
| --- | --- |
| `~/Downloads/la magnerie/LHD_FXX_0445_6806_MNH_O_0M50_LAMB93_IGN69` | 16,000,513 bytes; GTiff, 2000×2000 Float32, 0.5 m pixels, NoData −9999, strip layout and no overviews. SHA-256 `c4e2938e8a166b723f25c7ac3f5a64d21555671b758fb3b40b71daac45a8bc28`. Numeric height import/display, not ground slope. |
| `~/Downloads/la magnerie/` | 12 inspected MNH tiles, X 0445–0448 and Y 6806–6808; total 192,006,156 bytes; aligned 8000×6000 union, 48 million cells. Must import as one batch and reopen/display successfully. |
| `~/Downloads/LHD_FXX_0445_6806_MNT_O_0M50_LAMB93_IGN69/LHD_FXX_0445_6806_MNT_O_0M50_LAMB93_IGN69` | 16,000,513 bytes; matching 2000×2000 grid/type/NoData. SHA-256 `7b8773046c27d3f42d9f6548c7adc24fca810de19ea6ed5f49b29302e22076ad`. Ground-elevation slope fixture. |

Both named files have an imperfect embedded WKT declaring EPSG:2154 with incomplete datum detail. Accompanying export JSON identifies LAMB93/IGN69, acquisition 2025-02-15, edition 2025-07-23. Qualification must verify CRS interpretation numerically; do not globally trust filename text or silently substitute a datum. Capture the sidecar identity and engine interpretation in evidence.

Build a separate disposable 20,000×20,000 Float32 analytical-plane GeoTIFF (400 million cells, approximately 1.6 GB uncompressed) with independently known slope, holes and explicit NoData. Use 0.5 m pixels and `z=0.25*x+0.5*y-100` in local metres; generate in bounded blocks, disable compression for this capacity input, and assert actual file size exceeds 1 GiB. Include holes crossing processing-block boundaries. It tests a single file above the current byte/cell ceilings; do not resample user data and describe that as a new real survey. Also test at least 24 small adjacent/separated tiles, with a million-pixel gap in one arrangement; allocated blocks must track occupied coverage rather than the bounding rectangle. No genuine drone survey is presently supplied; drone-specific format/accuracy claims remain unqualified.

Proposed release gates for bounded processing: the supplied 48-million-cell batch and the synthetic 400-million-cell single file complete import/reopen; the latter also completes slope without a whole-grid allocation. Use an isolated profile on a recorded machine with at least 8 GiB RAM and sufficient temporary disk. Limit combined raster-job worker/process working memory to 1 GiB, excluding the measured idle app baseline; bound concurrent reads and record peak RSS, Wasm heap, temporary bytes, and queue depth. If library behavior cannot meet this budget, stop at the qualification gate and revise the engine plan explicitly. Never raise the old ceilings alone and call the work complete.

Measure one cold run and three warm runs per real display fixture; record median and p95 tile latency plus frame/long-task observations during navigation. Existing 55 fps and tile-latency aspirations in the earlier plan remain diagnostic until representative hardware is accepted; they are not measured promises. Memory bounds, responsive cancellation, correctness, and no UI-thread raster compute above 50 ms are release gates. Cancellation should stop scheduling immediately and settle owned work within 5 seconds in the fixture tests; test stalled subprocess/worker termination too. Record machine, OS/WebView, dependency artifacts, resolutions, raw measurements, and failures. Test timing does not replace lifecycle assertions.

### Q: historical qualification evidence

The old Q experiment interface, engine-selection queue and tooling-migration instructions are retired; their exact text is retained in Git at `71abad0f`, and their outcomes remain in the [Q receipt](raster-qualification-q.md). The [reset](raster-rework/qualification-reset.md) froze an incomplete instrument, not production correctness obligations.

The user-selected [GeoLibre native integration](raster-rework/geolibre-integration-design.md) now owns the first production verification boundary: pinned native dependency, real import/analysis callers, compatible bytes and masks, bounded extraction, cancellation and retained publication. Later capacity/display slices own whole-workflow memory, sparse coverage, caches and platform evidence before making those claims. No requirement is retrospectively marked passed.

### Qualification tooling language and migration

Rust owns native raster work; TypeScript owns any newly needed orchestration. Existing qualification Python and TypeScript tooling stays frozen, with known defects and useful regressions retained. Do not repair, rerun or port it merely to complete a checklist. Unrelated Python repository tools remain unchanged. No new qualification framework, decision evaluator or browser transport is required by G1–G5.

### Q experiment interface and evidence

Historical commands are evidence only, not current execution authority. Do not repin historical manifests to match the new native build, relabel old records or treat exploratory browser results as production/native acceptance. Production test inputs and build identity belong in the new integration receipt. Use ordinary focused tests and an isolated application workflow; the current assignment names authorized fixtures and limits.

## Acceptance examples

| Starting state and action | Observable result |
| --- | --- |
| Empty library; import supplied MNH batch | One height dataset with 48-million-cell extent, preserved valid samples, usable tiled display, restart reuse; no preview or fixed dense-grid rejection |
| Existing value 5; incoming valid 9, replacement off/on | Value stays 5 / becomes 9; unaffected regions remain identical |
| Existing valid pixel; incoming NoData | Existing pixel survives either choice; valid zero and negative inputs remain data |
| Batch contains corrupt or incompatible source | Named error; no partial head publication; prior Design/library usable |
| Import interrupted during prepare or publication | No acknowledged partial generation; restart recovery converges; originals unchanged |
| Source head changes before a job publishes | Stale job cannot overwrite newer state; clear retry/refresh outcome |
| Hidden ground dataset; run slope | Compatible input remains selectable; visible result independent of source; saved result reused after restart |
| Select MNH for slope | Rejected as incompatible measurement, not merely accepted because it is TIFF |
| Plane fixture crosses processing blocks | Correct interior and seam values/validity against independent expectations; no artificial tile-edge gaps |
| Import A=5, replace with B=9, reimport A with replacement on | Original A bytes reused; value returns to 5 through a new member occurrence; undo that last import returns to 9 |
| Valid batch resolves to identical values/mask | `noChange`, same head and history, no automatic refresh or new Design entry |
| Two imports or undo/import target one dataset | Second request returns busy; first retains sole publication ownership |
| Delete while work or numeric reads are in flight | Jobs cancel/settle, leased reads finish or cancel safely, late completion cannot recreate the deleted entity |
| Migration of v6 library with pending review and saved Design | IDs/history and original hashes preserved, review aborted without Apply, saved references still resolve |
| Pan, restyle, hide result | No recomputation or numeric change; hidden result stops unnecessary display reads |
| Library rename, then save/reopen Design | Stable references and results; rename alone does not dirty Design |
| Remove reference, then undo | Presentation restored; library bytes/results never deleted |
| Switch Design while import/analysis completes | Library result may publish; no layer is attached to the wrong session |
| Web coordinate placement then undo/export/reopen | Correct spatial frame and history; local plant coordinates and raster geography unchanged |
| Google Satellite selected with no key | Keyless Google raster source selected; exact non-blocking key prompt visible; attribution retained |
| Google key supplied, changed, or removed | Official satellite session used when configured; stale responses fenced; removing key returns to keyless; camera and other layers preserved |
| Legacy settings select `satellite` without MapTiler key | Saved MapTiler choice retained and marked unavailable; it never becomes Google or street implicitly |
| Official viewport/session response arrives after provider change | No obsolete source, copyright, timer or error replaces current provider state |
| Synthetic provider error contains a fake API key | Key absent from console capture, diagnostic bundle, Design export and evidence output |
| Invalid Google key or provider outage | Actionable error, no silent official-to-keyless downgrade, other app features usable; credentials absent from exports/logs |
| Map failure during Web placement | Coordinate input remains usable; no native call; no silent confirmation from camera movement |
| Dataset tile failure or missing reference | Honest dataset-local unavailable state; remaining layers and canvas edits usable |
| Full-opacity geographic layers at DPR 1/2 and nonzero bearing | Grid, plant symbols, zones, guides and interaction stay visible above them; geographic rows never become drawing targets |

Expected outcomes derive from user-approved simple import and edition scope, ADR 0025, and retained library/document invariants. Deliberately replace old preview assertions; retain their atomicity, validity, and cancellation coverage through the new interface.

## Ordered execution and exit gates

Continue the [product-closure](raster-rework/bounded-generation-completion-agent-prompt.md) serially on `canopi-jv8a.4`, with one consolidated review; no subagents. G1–G5 is accepted at `a5fc7d7b`; the delivered bounded-generation branch starts this correction at `6a5130b3`. The table below retains product outcomes, not a second executable sequence: B1–B5 combines its storage/caller dependencies while deliberately retaining existing review/Apply and deferring new import/definition UI. `canopi-j8mp` remains the workbench parent and `canopi-5neg` deferred analysis expansion. Do not create a competing epic or claim deferred product outcomes from the foundation batch.

| Slice and prerequisite | Outcome and owned files/modules | Required exit evidence / stop |
| --- | --- | --- |
| G1–G5 — current assignment | GeoLibre-selected native reader, import extraction, slope postprocessing, unchanged workflow compatibility, documentation. | The settled integration design owns exact tests and boundaries; existing capacity limits stay. |
| Q — frozen historical work | Existing isolated tooling and evidence retained; explicit fixture selection already exists. | Unqualified; no longer a prerequisite or next assignment. Known defects remain recorded. |
| P — accepted HTML reference revision `0e696722`; independent of Q | `desktop/web/ui-gallery/raster-reference-prototype/` is the proposal; use the HTML UI reference contract above. No native mutations or production registrations. Review/revise these files, not the old LiDAR study. | Receipt: user approval on 2026-09-18 of all seven routes/states; authoring checks recorded in `canopi-j8mp.1`. Reuse this accepted revision. Material changes require renewed review. Production localization is additionally verified in U/W/B. |
| N1 — G1–G5 accepted and next foundation design forwarded | `lidar/{catalogue,paths,grid,probe}.rs`, generation resolver, minimal `mod.rs` composition, focused native tests. Implement versioned prepared assets, storage migration and legacy read adapter. | Old v6 library and new sparse generation expose identical window/mask/history behavior; atomic migration/backup and future-schema rejection pass; no whole-grid allocation. No new import UI or old-reader deletion. |
| N2 — N1 | `lidar/{import,mod}.rs`, necessary catalog job metadata, `commands/lidar.rs`, `common-types/src/lidar.rs`, IPC wrapper and generated outputs. Add the single-action import receipt/scheduler and bounded undo. | Real batch/large-file import and reopen within resource gates; order/reimport/no-op, corrupt file, busy/deletion races, disk exhaustion and crash at each publication boundary. Old head remains usable. No preview/apply stage in the new interface. |
| N3 — N2 | `lidar/analysis.rs`, persistent named definitions/revisions, scheduler refresh composition and shared result contracts. | Both slope units/quality masks match oracles across blocks; real MNT and large plane pass; restart reuses results; repeated source changes coalesce and stale completions never publish. Failure retains prior result. |
| D — N3 and settled display design | `lidar/display.rs`, production-tested native display adapter, IPC needed for display, `app/canvas-map-surface/{lidar-sync,layer-stack,workspace-map-contributions}.ts`, tile URL/presentation adapters and focused tests. No browser engine is preselected by Q. | On-demand tiles, bounded cache/queue, styles/legends and local error state; retained camera/context/order at DPR 1/2 and bearing; cancellation/partial-init/teardown/offline assets. No science on pan/restyle. |
| U — D and explicit P approval | Production Data/Analysis/Layers components, `app/lidar/{actions,library-store}.ts`, Design Edit references, Desktop dock/shell registrations, accepted gallery fixtures and all 11 locale changes for this surface. | Complete import → slope → hide source/show result → reopen/new-Design reuse. Session-fenced attachment, jobs survive panel switches, hide/detach/delete differ, unknown Web entries round-trip. Remove obsolete preview UI/IPC/asset allowances only after replacement behavior tests pass. |
| I — U | Native one-pixel query/IPC, app inspection mode and canvas command adapter, Layers inspection controls, styles/legend persistence tests. | Known numeric value differs from colorized pixel yet reports correctly; NoData, containing-pixel edges, keyboard sampling, gesture ownership, stale responses and disposal pass. No mirrored Scene or numeric state. |
| W — I, P approval includes Web mock states | Browser-safe Location view and optional Desktop search composition, Web shell registration, coordinate tests and emitted Web import guards. Replacement Web scope ADR and affected guides. | Coordinate/map confirmation, Cancel/Escape, Design replacement, undo/draft/export/reopen and map-failure entry pass in both editions; Desktop address search retained. No native search import in Web. |
| B — W | `common-types/src/settings.rs`, settings projection/adapters/generated outputs, shared `maplibre/` provider owner, Location and Canvas contribution updates, provider controls/locales and diagnostic redaction tests. | Legacy MapTiler migration, keyless and official Google flows, bounded retries, expiry, viewport attribution, redaction and source-switch races; no map/scene recreation. Mock tests plus isolated live keyed/keyless receipts; missing live access remains a named gate. |
| F — every prior slice verified and combined | Release/build assets, offline engine/worker packaging, combined integration tests, evidence, operating docs and backlog reconciliation. No new product behavior. | Combined current-main checks, Linux/Windows/macOS artifact matrix, raw measurement receipts and fresh diff/contract review. Unsupported/unavailable platforms block their release claims; do not close the entire epic as qualified with missing required evidence. |

Each slice starts by rerunning its focused baseline tests and reading only its owned subsystem guides plus dependencies' receipts. Contract changes and regenerated files stay in the same commit; locale edits are batched within the owning UI/provider slice. Do not combine a failed baseline repair with the feature; use the repository's separate maintenance workflow. Add neither a second engine abstraction nor an unplanned service to make a slice pass.

B1–B5 migrates existing commands to the bounded reader before publishing new storage; existing review/Apply, undo, display and slope remain usable on both versions. Never pass a new head into a dense helper. U later switches the interaction to the new single-action flow. U retires the old creation/review/apply/preview entry points, migrates meaningful regression assertions, and removes exact obsolete asset permissions. Preserve legacy storage readers and history, even after the obsolete interaction is removed. Each intermediate commit must build and retain existing supported user flows. If a proposed migration breaks an existing caller, add and verify its compatibility adapter in the owning slice before closing it; do not merge an unusable intermediate state or expand beyond the explicitly combined B1–B5 batch. The new single-action import is not exposed to users until U.

Hydrology/canopy/globe follow-ups start only after the core path passes. Hydrology needs explicit conditioning, upstream-domain completeness, nonlocal dependency handling and independent numerical evidence; canopy needs declared input semantics; globe needs a renderer/capability decision and preserves local editing authority. Do not elaborate or implement those contracts past their own decision gates.

## Verification, documentation, and handoff

Use existing meaningful coverage in `desktop/web/src/__tests__/lidar-actions.test.ts`, `lidar-library-store.test.ts`, `lidar-import-progress.test.tsx`, `lidar-map-presentation.test.ts`, `lidar-camera-navigation.test.ts`, `design-edit-lidar.test.ts`, `location-workbench.test.tsx`, `location-route-boundary.test.ts`, and `workspace-composition.test.tsx`. Update behavior assertions instead of preserving obsolete UI flows or weakening architecture guards. Native tests live with the LiDAR modules; extend `e2e.rs` to accept an explicit fixture path rather than silently choosing another Downloads tile.

Apply the [repository quality gates](../../AGENTS.md#quality-gates) for this mixed Rust/frontend/persistence/shared-contract change: Rust formatting, strict Clippy, workspace check/tests, native command policy, bindings regeneration/drift checks, TypeScript, full Vitest, gallery check, both edition builds, and documentation validation. The [edition guide](../agent/edition-development.md#verification-workflow) owns runnable commands and environment boundaries. Record actual commands and results beside production tests; do not build a phase-1 harness to satisfy documentation.

Use `npm run dev:ui` for live prototype/production review, then isolated real Desktop and Web sessions as described in the edition guide. Browser/jsdom success does not establish native file access, WebKit behavior, or packaged WASM loading. Qualify Linux, Windows, and macOS before claiming support there. Pin/bundle required offline processing assets and test worker/CSP/asset paths without network; a CDN-only successful development run is insufficient.

At implementation, update `CONTEXT.md`, the dock design contract, LiDAR/MapLibre/edition/build-release/document-format guides, and the old LiDAR design's current-guidance/status links. The foundation record already retires preview/region-choice and no-pixel-inspection as future requirements; replace current operating guidance only when their implementation changes. Keep scientific validity, independent visibility, immutable publication, and history requirements. Existing Web map-scope ADR descriptions contain historical implementation statements; replace their scope through a linked decision rather than treating those statements as current behavior. Do not change operating guidance merely because this proposal exists.

Use the [issue workflow](../workflow/issue-tracker.md) and [delivery contract](../workflow/delivery.md). At handoff, attach fixture identities, numeric/resource measurements, exact validation results, upstream artifact pins, and unavailable platform evidence to the normal bead/commit receipt. No implementation or new issue creation is authorized by this proposal.

### Per-slice command and receipt contract

The commands below exist at the inspected baseline. Run the focused checks during implementation and the repository-required gates for changed surfaces before closing a slice. Do not use successful unit tests as a substitute for required product/resource/platform measurements or P approval.

| Changed surface | Commands and working directory |
| --- | --- |
| Native library (N1–N3 and native parts of D/I/B) | At root: `CANOPI_SKIP_BUNDLED_DB=1 cargo test -p canopi-desktop services::lidar` for focused library coverage; `cargo fmt --all -- --check`; `CANOPI_SKIP_BUNDLED_DB=1 cargo clippy --workspace --all-targets -- -D warnings`; `CANOPI_SKIP_BUNDLED_DB=1 cargo check --workspace`; `CANOPI_SKIP_BUNDLED_DB=1 cargo test --workspace`. |
| Native command/executor changes | At root: `CANOPI_SKIP_BUNDLED_DB=1 cargo test -p canopi-desktop native_command_policy::tests`. |
| Shared types/settings | In `desktop/web`: `npm run gen:types` then `npm run check:types`; commit generated outputs with authored contracts. |
| Frontend, gallery and edition composition (P/D/U/I/W/B) | In `desktop/web`: `npx tsc --noEmit`, focused `npx vitest run` with the affected test filenames, `npm run check:ui`, `npm run build`, `npm run build:web`. Run `npm test` for shared runtime, lifecycle or broad frontend changes. P additionally requires `npm run dev:ui` and the explicit acceptance receipt. |
| Documentation (every affected slice) | At root: `python3 scripts/check_docs.py` and `git diff --check`. |
| Private real lifecycle | Use the existing explicit `CANOPI_LIDAR_E2E_FIXTURE` mechanism and exact test `CANOPI_SKIP_BUNDLED_DB=1 cargo test -p canopi-desktop services::lidar::e2e::e2e_import_publish_slope_restart_reuse -- --ignored --exact --nocapture`, with a fixture whose identity matches the assertions. This alone is not the capacity harness. |

For focused frontend tests use the existing filenames listed above, plus `settings-platform-adapters.test.ts`, `settings-projection.test.ts`, `maplibre-config.test.ts`, `location-map-editing-host.test.tsx`, `problem-report-diagnostics.test.ts`, and the colocated `workspace-map-controls.test.ts` and `workspace-runtime-composition.test.ts`. Add behavior tests for new numeric/worker/provider seams; do not invent passing test names in the receipt. `npm run build:web` runs the existing emitted-boundary scanner; follow the edition guide's packaging checks if asset/base-path behavior changes.

Each completion receipt includes starting and final revision, bead/branch, changed contracts, dependency receipts, exact commands/results, fixture and artifact identities, resource/numeric measurements, retained legacy behavior, agent-doc updates, and named unavailable evidence. P records the user-approved revision and gallery routes. The native integration receipt records its actual dependency/build and user-selected route. F records ancestry of all slice commits in the delivered checkout and a platform-by-platform pass/fail/unavailable table. A reviewer must compare the final changes with this plan and the meaningful old tests, including crash boundaries, unknown-field preservation and map lifecycle; review is a separate pass, not a claim that green tests prove design correctness.

## Open gates and plan audit

The current production assignment is B1–B5 under `canopi-jv8a`, as settled in the bounded-generation design. The former Q→N1 tooling prerequisite is superseded, not passed. The accepted native integration remains its starting point; sparse generations and whole-workflow capacity gates are now in scope, with new UI still deferred. P is satisfied by approval of `0e696722`; faithful later implementation needs no repeat approval. Changes outside the forwarded design's UI, engine or scientific contracts still return through the user.

S1 remains the selected Google behavior with its recorded service limitation. The plan fixes provider storage, migration, session/attribution ownership and error behavior; it makes no new authorization or reliability claim for the keyless endpoint. Exact pinned upstream artifacts, local WASM transport feasibility, live restricted-key behavior, and packaged-platform results remain evidence to produce, not verified outcomes of this planning revision. Larger real drone data is unavailable; synthetic scale tests cannot establish drone-specific claims. The plan revision changes no operating guide's description of current production behavior.
