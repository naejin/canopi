{
  "name": "canopi-raster-qualification",
  "private": true,
  "type": "module",
  "description": "TypeScript qualification decision path for the raster rework (slice Q). Emitted output is disposable; commit source and configuration only.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test \"dist/tests/*.test.js\""
  }
}
