[alias]
# Rust coverage ratchet: the floor is the measured baseline; raise it, never lower it.
cov = "llvm-cov --workspace --locked --summary-only --fail-under-lines 69.4 --fail-under-functions 63.4 --fail-under-regions 68.4"
